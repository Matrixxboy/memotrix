{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Memotrix","text":"<p>Memotrix is a Python library that gives an agent long-term memory over files and free-text facts. You pick embeddings, pick a store, then call <code>add</code> / <code>add_text</code> / <code>search</code> / <code>delete</code>.</p> <p>It is not a hosted service, not a chat UI, and not an LLM. You retrieve chunks, then you pass those chunks into your own model.</p> <pre><code>from memotrix import Memory\nfrom memotrix.embeddings import HuggingFaceEmbeddings\nfrom memotrix.vectorstores import InMemoryStore\n\nembeddings = HuggingFaceEmbeddings(model=\"BAAI/bge-small-en-v1.5\")\nmemory = Memory(embeddings=embeddings, store=InMemoryStore(embeddings))\n\nmemory.add_text(\"User prefers dark mode.\", memory_type=\"semantic\", source_id=\"prefs\")\nhits = memory.search(\"what theme does the user want?\", top_k=3)\nmemory.close()\n</code></pre>"},{"location":"#what-it-does","title":"What it does","text":"You need Memotrix does Agent long-term memory <code>add_text</code> for facts, chat turns, procedures (<code>semantic</code> / <code>episodic</code> / <code>procedural</code>) RAG over files <code>add(\"report.pdf\")</code> then <code>search(\"what is the revenue?\")</code> Hybrid retrieval Dense vectors (HNSW or pgvector) + sparse keywords (BM25 or Postgres <code>tsvector</code>), fused with RRF Tight context Default <code>top_k=3</code> and neighbor-window expansion, not five full files Explicit config You pass the embedding model and DSN. Nothing is silently defaulted."},{"location":"#install","title":"Install","text":"<pre><code>pip install memotrix\n</code></pre> <p>A bare install only includes <code>python-dotenv</code>. To construct <code>Memory</code> you need an extra:</p> <pre><code>pip install \"memotrix[memory]\"\n</code></pre> <p>See Installation for every extra (<code>postgres</code>, <code>extractors</code>, <code>openai</code>, <code>audio</code>, <code>all</code>).</p>"},{"location":"#documentation-map","title":"Documentation map","text":"Section Start here Get started Installation \u00b7 Quickstart \u00b7 Configuration How it works Concepts \u00b7 Architecture \u00b7 Hybrid search Every use case Use cases API <code>Memory</code> \u00b7 Embeddings \u00b7 Stores Live demos DemoCodeLive Internal architecture notes docBook"},{"location":"#public-api-surface","title":"Public API surface","text":"<p>From <code>memotrix</code>:</p> <ul> <li><code>Memory</code>, <code>MemoryConfig</code>, <code>build_retrieval_stack</code></li> <li><code>HuggingFaceEmbeddings</code>, <code>OpenAIEmbeddings</code>, <code>FakeEmbeddings</code>, <code>Embeddings</code></li> <li><code>InMemoryStore</code>, <code>PostgresStore</code></li> <li><code>ConfigurationError</code></li> </ul> <p>Package version on PyPI: 0.2.0. Source in this repo: 0.2.1 (Pillow no longer required to import <code>Memory</code>; <code>__version__</code> matches <code>pyproject.toml</code>). Python 3.10+. License: MIT.</p>"},{"location":"#live-demo-project","title":"Live demo project","text":"<p>Runnable scripts that import the PyPI package (not this repo\u2019s <code>src/</code>):</p> <p><code>DemoCodeLive/</code></p> <pre><code>cd DemoCodeLive\npython -m venv .venv\n.venv/Scripts/activate   # Windows\npip install \"memotrix[memory]\"\npython run_all.py\n</code></pre>"},{"location":"PUBLISHING/","title":"Publishing Memotrix to PyPI","text":"<p>This is the release checklist for the <code>memotrix</code> package (the <code>memory/</code> directory). Do not upload <code>.env</code> files, <code>__pycache__</code>, or private test fixtures.</p> <p>Current version is declared in two places and must stay in sync:</p> <ul> <li><code>pyproject.toml</code> \u2192 <code>[project] version</code></li> <li><code>src/memotrix/__init__.py</code> \u2192 <code>__version__</code></li> </ul>"},{"location":"PUBLISHING/#1-preconditions","title":"1. Preconditions","text":"<ol> <li>A PyPI account (and a TestPyPI account for the dry run).</li> <li>Confirm the name is free:</li> </ol> <pre><code>pip index versions memotrix\n</code></pre> <p>If the name is taken, change <code>[project] name</code> in <code>pyproject.toml</code> before the first upload. You cannot rename a project after it exists on PyPI.</p> <ol> <li>Homepage / source URL in <code>pyproject.toml</code> (<code>[project.urls] Homepage</code>) must be a real repository. Today it is <code>https://github.com/memotrix/memotrix</code> \u2014 replace it if that org/repo is not yours.</li> <li>Set a real author (and email if you want) under <code>[project] authors</code>.</li> <li><code>LICENSE</code> (MIT) lives next to <code>pyproject.toml</code>. Keep <code>license = { file = \"LICENSE\" }</code>.</li> <li>Python <code>&gt;=3.10</code> (<code>requires-python</code>). Users must pass extras (<code>[memory]</code>, <code>[postgres]</code>, \u2026); a bare <code>pip install memotrix</code> only installs <code>python-dotenv</code>.</li> </ol>"},{"location":"PUBLISHING/#2-what-gets-published","title":"2. What gets published","text":"<p>The sdist / wheel is built from <code>memory/</code> with <code>setuptools</code> (<code>packages.find</code> where <code>src/</code>). Included:</p> <ul> <li><code>memotrix</code> package under <code>src/</code></li> <li><code>py.typed</code></li> <li><code>pypi.md</code> (as the PyPI long description / About panel)</li> <li><code>LICENSE</code></li> </ul> <p>Not included (and should stay out): <code>.env</code>, test files, eval JSON, <code>__pycache__</code>, local media folders (<code>.memotrix_media</code>).</p> <p>Users then install extras:</p> <pre><code>pip install memotrix[memory]\npip install memotrix[postgres,memory,extractors]\npip install memotrix[all]\n</code></pre>"},{"location":"PUBLISHING/#3-version-bump","title":"3. Version bump","text":"<p>Before each release, increment both:</p> <pre><code># pyproject.toml\nversion = \"0.2.1\"\n</code></pre> <pre><code># src/memotrix/__init__.py\n__version__ = \"0.2.1\"\n</code></pre> <p>Use semver: patch for bugfixes, minor for compatible API additions, major for breaking changes. You cannot reuse a version that was already uploaded to PyPI.</p>"},{"location":"PUBLISHING/#4-build-locally","title":"4. Build locally","text":"<p>From <code>memory/</code> (the directory that contains <code>pyproject.toml</code>):</p> <pre><code>python -m pip install --upgrade build twine\npython -m build\npython -m twine check dist/*\n</code></pre> <p>Expect <code>dist/memotrix-&lt;version&gt;.tar.gz</code> and <code>dist/memotrix-&lt;version&gt;-py3-none-any.whl</code>.</p> <p>Install the wheel in a clean venv to sanity-check:</p> <pre><code>python -m venv .venv-release\n# Windows: .venv-release\\Scripts\\activate\n# Unix:    source .venv-release/bin/activate\npip install dist/memotrix-*.whl\npython -c \"import memotrix; print(memotrix.__version__)\"\n</code></pre>"},{"location":"PUBLISHING/#5-testpypi-first-required-dry-run","title":"5. TestPyPI first (required dry run)","text":"<p>Create an API token on TestPyPI (Account \u2192 API tokens). Prefer Trusted Publishing (section 7) over long-lived tokens.</p> <pre><code>python -m twine upload --repository testpypi dist/*\n</code></pre> <p>Install from TestPyPI (dependencies that live only on real PyPI may need <code>--extra-index-url</code>):</p> <pre><code>pip install -i https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple memotrix[memory]\n</code></pre> <p>Smoke-test:</p> <pre><code>from memotrix import Memory\nfrom memotrix.embeddings import FakeEmbeddings\nm = Memory(embeddings=FakeEmbeddings(dim=8), backend=\"memory\")\nm.add_text(\"hello\", source_id=\"t\")\nprint(m.search(\"hello\"))\nm.close()\n</code></pre>"},{"location":"PUBLISHING/#6-production-pypi","title":"6. Production PyPI","text":"<p>When TestPyPI looks right:</p> <pre><code>python -m twine upload dist/*\n</code></pre> <p>Do not pass <code>--skip-existing</code> to hide a failed upload of a new version. If a version is broken on PyPI, yank it in the web UI and ship a new version number.</p> <p>After upload:</p> <pre><code>pip install memotrix[memory]\n</code></pre>"},{"location":"PUBLISHING/#7-trusted-publishing-recommended-vs-api-tokens","title":"7. Trusted Publishing (recommended) vs API tokens","text":""},{"location":"PUBLISHING/#api-token-manual","title":"API token (manual)","text":"<ul> <li>Create a scoped token on pypi.org (project: <code>memotrix</code> once it exists, or account-wide for the first upload).</li> <li>Store it in a password manager. Never commit it. Use:</li> </ul> <pre><code>python -m twine upload dist/*\n# prompts, or set TWINE_USERNAME=__token__ and TWINE_PASSWORD=&lt;pypi-token&gt;\n</code></pre>"},{"location":"PUBLISHING/#trusted-publishing-github-actions-oidc","title":"Trusted Publishing (GitHub Actions OIDC)","text":"<p>PyPI can accept uploads from GitHub without a stored token:</p> <ol> <li>On pypi.org \u2192 your project \u2192 Publishing \u2192 add a GitHub publisher (<code>owner</code>, <code>repository</code>, <code>workflow</code> filename, optional <code>environment</code>).</li> <li>Add a workflow at the repo root (this repo has no <code>memory/.github</code>; a root workflow that <code>cd memory</code> and builds is enough). Example sketch \u2014 do not invent an org name; fill in yours:</li> </ol> <pre><code># .github/workflows/publish-memotrix.yml\nname: Publish memotrix\non:\n  release:\n    types: [published]\npermissions:\n  id-token: write\n  contents: read\njobs:\n  pypi:\n    runs-on: ubuntu-latest\n    environment: pypi\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n      - run: pip install build\n      - run: python -m build\n        working-directory: memory\n      - uses: pypa/gh-action-pypi-publish@release/v1\n        with:\n          packages-dir: memory/dist\n</code></pre> <ol> <li>First production upload can use a one-time token if the project does not exist yet; subsequent releases use OIDC.</li> </ol> <p>Never put <code>TWINE_PASSWORD</code> in the workflow YAML or in git.</p>"},{"location":"PUBLISHING/#8-release-checklist","title":"8. Release checklist","text":"<ul> <li>[ ] Tests pass from <code>memory/</code>: <code>pytest tests/test_memory_sdk.py tests/test_config.py tests/test_hybrid_search.py tests/test_neighbor_expansion.py tests/test_domain_extractors.py -q</code></li> <li>[ ] <code>version</code> in <code>pyproject.toml</code> equals <code>__version__</code></li> <li>[ ] Homepage URL is correct; <code>LICENSE</code> present</li> <li>[ ] <code>python -m build</code> + <code>twine check dist/*</code></li> <li>[ ] TestPyPI install + import smoke test</li> <li>[ ] Production upload (Trusted Publishing or token)</li> <li>[ ] Git tag <code>vX.Y.Z</code> on the commit you released</li> <li>[ ] Delete local <code>dist/</code> / <code>*.egg-info</code> if you do not want artifacts in the working tree</li> </ul>"},{"location":"PUBLISHING/#9-what-not-to-publish","title":"9. What not to publish","text":"<ul> <li><code>.env</code>, credentials, <code>OPENAI_API_KEY</code></li> <li><code>tests/filesForTests</code> and eval dumps (they are not in the package, keep it that way)</li> <li><code>__pycache__</code>, <code>.pytest_cache</code></li> <li>A second project name from this repo (<code>agent/</code> is not this package)</li> </ul>"},{"location":"configuration/","title":"Configuration","text":"<p>Memotrix never invents a database password, host, embedding model, or vector dimension. Missing values raise <code>ConfigurationError</code>.</p>"},{"location":"configuration/#constructing-memory","title":"Constructing Memory","text":"<pre><code>from memotrix import Memory, MemoryConfig\nfrom memotrix.config import ChunkingConfig, RetrievalConfig, IngestConfig\nfrom memotrix.embeddings import HuggingFaceEmbeddings\nfrom memotrix.vectorstores import InMemoryStore, PostgresStore\n</code></pre> Constructor When <code>Memory(embeddings, store=InMemoryStore(...))</code> Explicit in-process store <code>Memory(embeddings, backend=\"memory\")</code> Same, store created for you <code>Memory(embeddings, store=PostgresStore(...))</code> Explicit Postgres <code>Memory(embeddings, connection=dsn)</code> Postgres inferred from <code>connection=</code> <code>Memory.from_config(config)</code> Dataclass tunables + env-style model name <code>Memory.from_env()</code> All required values from the environment <p><code>embeddings</code> may be an <code>Embeddings</code> instance or a HuggingFace model name string (<code>coerce_embeddings</code>).</p>"},{"location":"configuration/#environment-variables","title":"Environment variables","text":"<p>Used by <code>Memory.from_env()</code> / <code>MemoryConfig.from_env()</code> / <code>HuggingFaceEmbeddings.from_env()</code> / <code>resolve_database_url()</code>.</p> Variable Required when Purpose <code>EMBEDDING_MODEL</code> <code>from_env()</code> HuggingFace model id, e.g. <code>BAAI/bge-small-en-v1.5</code> <code>MEMOTRIX_BACKEND</code> optional <code>memory</code> (default) or <code>postgres</code> <code>DATABASE_URL</code> backend is <code>postgres</code> Postgres DSN <code>DATABASE_USER</code> / <code>DATABASE_PASSWORD</code> / <code>DATABASE_HOST</code> / <code>DATABASE_PORT</code> / <code>DATABASE_NAME</code> postgres without <code>DATABASE_URL</code> Discrete DSN parts (also <code>POSTGRES_*</code> aliases) <code>DATABASE_DIALECT</code> optional Default <code>postgresql</code> <code>MEMOTRIX_TABLE</code> optional Chunk table name, default <code>memotrix_chunks</code> <code>RERANKER_MODEL</code> optional Cross-encoder model name <code>OPENAI_API_KEY</code> <code>OpenAIEmbeddings</code> without <code>api_key=</code> OpenAI embeddings / vision <code>OPENAI_MODEL</code> optional Chat model for <code>OpenAIProvider</code> (not embeddings)"},{"location":"configuration/#memoryconfig-defaults","title":"MemoryConfig defaults","text":"<p>These are library defaults, not secrets:</p> Field Default Meaning <code>backend</code> <code>\"memory\"</code> <code>memory</code> or <code>postgres</code> <code>table_name</code> <code>\"memotrix_chunks\"</code> Postgres table <code>max_elements</code> <code>50000</code> HNSW capacity (in-memory; not resizable) <code>chunking.chunk_size</code> <code>500</code> Characters per chunk <code>chunking.chunk_overlap</code> <code>50</code> Overlap characters <code>retrieval.top_k</code> <code>3</code> Hits returned <code>retrieval.max_tokens</code> <code>800</code> Soft budget after neighbor expansion <code>retrieval.expand_mode</code> <code>\"neighbors\"</code> <code>neighbors</code> / <code>full_file</code> / <code>none</code> <code>retrieval.neighbor_window</code> <code>1</code> \u00b1 chunks around a hit <code>retrieval.enable_rerank</code> <code>True</code> Used only if a reranker model is set <code>retrieval.fusion_k</code> <code>60</code> RRF constant <code>retrieval.query_cache_size</code> <code>256</code> Cached query embeddings <code>retrieval.enable_memory_boost</code> <code>True</code> Recency / access / importance multiplier <code>ingest.enable_dedup</code> <code>True</code> Near-duplicate merge <code>ingest.dedup_threshold</code> <code>0.95</code> Cosine threshold <code>ingest.describe_images</code> <code>True</code> Vision/caption path for images <p>Example:</p> <pre><code>config = MemoryConfig(\n    backend=\"memory\",\n    max_elements=10_000,\n    chunking=ChunkingConfig(chunk_size=800, chunk_overlap=80),\n    retrieval=RetrievalConfig(top_k=5, expand_mode=\"none\", enable_memory_boost=False),\n    ingest=IngestConfig(enable_dedup=False),\n)\nmemory = Memory(embeddings=embeddings, config=config)\n</code></pre>"},{"location":"configuration/#custom-extractors-at-construction","title":"Custom extractors at construction","text":"<pre><code>memory = Memory(embeddings=embeddings, extract_file=my_router)\n</code></pre> <p><code>extract_file</code> must accept <code>(path, *, describe_images, generate_srt)</code> and return <code>DocumentData</code>. You can also register by extension: see Custom extractors.</p>"},{"location":"configuration/#docker-postgres","title":"Docker Postgres","text":"<p>This repo\u2019s <code>docker-compose.yml</code> starts <code>pgvector/pgvector:pg16</code>. Credentials come from your environment (<code>POSTGRES_USER</code>, <code>POSTGRES_PASSWORD</code>, <code>POSTGRES_DB</code>, <code>POSTGRES_PORT</code>). The compose file does not ship a default password.</p> <p>See Postgres persistence.</p>"},{"location":"installation/","title":"Installation","text":"<p>Python 3.10+ is required (<code>requires-python</code> in <code>pyproject.toml</code>).</p>"},{"location":"installation/#pypi","title":"PyPI","text":"<pre><code>pip install memotrix\n</code></pre> <p>That command installs the package and its only required dependency: <code>python-dotenv</code>. It does not install HNSW, BM25, Sentence-Transformers, Postgres drivers, or file extractors. In 0.2.0, importing <code>Memory</code> still loads Pillow via <code>utils.ai_integration</code>; install <code>pillow</code> or use <code>memotrix[images]</code>. <code>InMemoryStore</code> needs <code>hnswlib</code> from <code>[local]</code> / <code>[memory]</code> (Windows: MSVC to compile). Details: Troubleshooting.</p> <p>To actually construct <code>Memory</code> and run hybrid search:</p> <pre><code>pip install \"memotrix[memory]\"\n</code></pre> <p>Quotes are required in zsh and some other shells.</p>"},{"location":"installation/#extras","title":"Extras","text":"Extra Enables When you need it <code>memory</code> <code>local</code> + <code>embeddings</code> Minimum to construct <code>Memory</code> with local embeddings <code>local</code> <code>hnswlib</code>, <code>rank-bm25</code>, <code>numpy</code> In-process HNSW + BM25 (<code>InMemoryStore</code>) <code>embeddings</code> <code>sentence-transformers</code> <code>HuggingFaceEmbeddings</code> <code>postgres</code> <code>psycopg[binary]</code>, <code>pgvector</code> <code>PostgresStore</code> <code>extractors</code> PDF, Office, HTML, CSV/Excel, images, RDF, YAML <code>memory.add(\"file.pdf\")</code> for those formats <code>pdf</code> / <code>docx</code> / <code>pptx</code> / <code>excel</code> / <code>images</code> / <code>html</code> / <code>knowledge</code> / <code>yaml</code> Granular extractors Install only the formats you use <code>documents</code> <code>pdf,docx,pptx,excel,html</code> Office + HTML + PDF together <code>data</code> <code>excel,knowledge,yaml</code> Spreadsheets + RDF + YAML <code>all-extractors</code> documents + data + images Every wired extractor extra <code>openai</code> <code>openai</code> <code>OpenAIEmbeddings</code> and vision captions <code>audio</code> <code>faster-whisper</code> Audio / video transcription <code>all</code> postgres + local + embeddings + extractors + openai + audio Full install <code>dev</code> <code>pytest</code> Running the library test suite from source <p>Examples:</p> <pre><code>pip install \"memotrix[memory]\"\npip install \"memotrix[postgres,memory,extractors]\"\npip install \"memotrix[memory,pdf]\"\npip install \"memotrix[all]\"\n</code></pre>"},{"location":"installation/#what-works-without-extras","title":"What works without extras","text":"<p>These formats use the Python standard library (or code already in the wheel) once <code>[local]</code> is installed so indexes exist:</p> <ul> <li><code>.txt</code>, <code>.md</code>, <code>.json</code>, <code>.jsonl</code>, <code>.csv</code>, <code>.sql</code>, <code>.xml</code>, <code>.py</code> / <code>.js</code> / <code>.ts</code> / <code>.java</code> / <code>.go</code> / <code>.rs</code> / C++, <code>.log</code>, <code>.eml</code>, <code>.mbox</code>, <code>.geojson</code></li> </ul> <p>These need extras:</p> Format Extra PDF <code>pdf</code> or <code>extractors</code> DOCX / PPTX <code>docx</code> / <code>pptx</code> Excel <code>.xlsx</code> <code>excel</code> HTML / EPUB <code>html</code> (EPUB uses stdlib zip + HTML helper) YAML <code>yaml</code> Images <code>images</code> RDF / Turtle / GraphML <code>knowledge</code> Audio / video <code>audio</code> (and <code>ffmpeg</code> on PATH) OpenAI embeddings <code>openai</code> <p>Not supported: generic <code>.zip</code> (unless it is a SCORM package containing <code>imsmanifest.xml</code>), BIFF <code>.xls</code> (convert to <code>.xlsx</code>).</p>"},{"location":"installation/#from-this-repository-development","title":"From this repository (development)","text":"<pre><code>pip install -e \".[postgres,local,embeddings,extractors,openai]\"\n</code></pre> <p>For usage demos, prefer the PyPI install inside DemoCodeLive so you are testing the published wheel, not <code>PYTHONPATH</code> pointing at <code>src/</code>.</p>"},{"location":"installation/#verify","title":"Verify","text":"<pre><code>import memotrix\nfrom memotrix import Memory\nfrom memotrix.embeddings import FakeEmbeddings\n\nmemory = Memory(embeddings=FakeEmbeddings(dim=8), backend=\"memory\")\nprint(\"ok\", memotrix.__file__)\nmemory.close()\n</code></pre> <p>If this import resolves to a path under this repo\u2019s <code>src/memotrix</code>, you are not using the PyPI wheel. Use a clean virtualenv as shown in DemoCodeLive.</p>"},{"location":"quickstart/","title":"Quickstart","text":"<p>Five minutes from install to a search hit. Copy these snippets as-is.</p>"},{"location":"quickstart/#1-install","title":"1. Install","text":"<pre><code>pip install \"memotrix[memory]\"\n</code></pre>"},{"location":"quickstart/#2-remember-a-fact","title":"2. Remember a fact","text":"<pre><code>from memotrix import Memory\nfrom memotrix.embeddings import HuggingFaceEmbeddings\nfrom memotrix.vectorstores import InMemoryStore\n\nembeddings = HuggingFaceEmbeddings(model=\"BAAI/bge-small-en-v1.5\")\nmemory = Memory(embeddings=embeddings, store=InMemoryStore(embeddings))\n\nmemory.add_text(\n    \"User prefers dark mode and Python 3.12.\",\n    source_id=\"prefs\",\n    memory_type=\"semantic\",\n)\n\nhits = memory.search(\"What theme does the user prefer?\", top_k=3)\nfor hit in hits:\n    print(hit.get(\"score\"), hit.get(\"chunk_text\", \"\")[:200])\n\nmemory.delete(\"prefs\")\nmemory.close()\n</code></pre> <p><code>HuggingFaceEmbeddings</code> downloads the model the first time. For tests and demos that must run offline, use <code>FakeEmbeddings(dim=8)</code> instead (deterministic, not semantically meaningful).</p>"},{"location":"quickstart/#3-ingest-a-file","title":"3. Ingest a file","text":"<pre><code>memory.add(\"notes.txt\")\nhits = memory.search(\"what is Memotrix?\", top_k=3)\nprint(memory.list_sources())\n</code></pre> <p><code>add</code> detects the extension, extracts text, chunks it (default 500 characters, 50 overlap), embeds the chunks, and writes both the dense and sparse indexes.</p>"},{"location":"quickstart/#4-persist-with-postgres","title":"4. Persist with Postgres","text":"<pre><code>pip install \"memotrix[postgres,memory]\"\n</code></pre> <pre><code>import os\nfrom memotrix import Memory\nfrom memotrix.embeddings import HuggingFaceEmbeddings\nfrom memotrix.vectorstores import PostgresStore\n\nembeddings = HuggingFaceEmbeddings(model=os.environ[\"EMBEDDING_MODEL\"])\nmemory = Memory(\n    embeddings=embeddings,\n    store=PostgresStore(connection=os.environ[\"DATABASE_URL\"], embeddings=embeddings),\n)\n</code></pre> <p>Or <code>Memory.from_env()</code> when <code>EMBEDDING_MODEL</code> and (for Postgres) <code>DATABASE_URL</code> are set. See Configuration.</p>"},{"location":"quickstart/#5-live-demos","title":"5. Live demos","text":"<p>DemoCodeLive contains one script per use case, installed with <code>pip install memotrix</code>.</p>"},{"location":"quickstart/#6-plug-into-an-llm","title":"6. Plug into an LLM","text":"<p>Memotrix returns a list of hit dicts. You format <code>chunk_text</code> into a prompt. See Build a RAG agent.</p>"},{"location":"quickstart/#next","title":"Next","text":"<ul> <li>Every use case</li> <li>Memory API</li> <li>DemoCodeLive</li> </ul>"},{"location":"api/config/","title":"Config API","text":"<pre><code>from memotrix import MemoryConfig\nfrom memotrix.config import (\n    ChunkingConfig,\n    RetrievalConfig,\n    IngestConfig,\n    resolve_database_url,\n    require_env,\n)\n</code></pre> <p>See Configuration for defaults and environment variables.</p> <p><code>resolve_database_url(connection=None)</code> uses <code>connection</code>, else <code>DATABASE_URL</code>, else discrete <code>DATABASE_*</code> / <code>POSTGRES_*</code> parts. It never invents a password.</p>"},{"location":"api/embeddings/","title":"Embeddings","text":"<pre><code>from memotrix.embeddings import (\n    Embeddings,\n    HuggingFaceEmbeddings,\n    OpenAIEmbeddings,\n    FakeEmbeddings,\n    coerce_embeddings,\n)\n</code></pre> <p>All implementations must provide <code>embed_documents</code>, <code>embed_query</code>, and <code>dimension</code>.</p>"},{"location":"api/embeddings/#huggingfaceembeddingsmodel-query_instructionnone","title":"<code>HuggingFaceEmbeddings(model, *, query_instruction=None)</code>","text":"<p>Requires <code>memotrix[embeddings]</code> / <code>[memory]</code>. Dimension from <code>get_sentence_embedding_dimension()</code> (or a probe encode). Query text may be prefixed with a BGE-style instruction via <code>format_query_for_embedding</code> unless <code>query_instruction</code> is set.</p> <p><code>HuggingFaceEmbeddings.from_env()</code> reads <code>EMBEDDING_MODEL</code>.</p> <p>Passing a string to <code>Memory(embeddings=\"BAAI/bge-small-en-v1.5\")</code> calls <code>coerce_embeddings</code> \u2192 <code>HuggingFaceEmbeddings</code>.</p>"},{"location":"api/embeddings/#openaiembeddingsmodel-api_keynone","title":"<code>OpenAIEmbeddings(model, *, api_key=None)</code>","text":"<p>Requires <code>memotrix[openai]</code> and <code>api_key</code> or <code>OPENAI_API_KEY</code>. Dimension inferred from the first API response. Batches documents in groups of 500.</p>"},{"location":"api/embeddings/#fakeembeddingsdim","title":"<code>FakeEmbeddings(dim)</code>","text":"<p>Deterministic hash-like vectors. For tests and DemoCodeLive. Not semantically meaningful.</p>"},{"location":"api/embeddings/#custom-embeddings","title":"Custom embeddings","text":"<p>Subclass <code>Embeddings</code> and implement the three members. Pass the instance to <code>Memory</code> and to <code>InMemoryStore</code> / <code>PostgresStore</code> so the index width matches.</p>"},{"location":"api/exceptions/","title":"Exceptions","text":"<pre><code>from memotrix.utils.exceptions import (\n    ConfigurationError,\n    DocumentExtractionError,\n    UnsupportedDocumentTypeError,\n    EmptyDocumentError,\n    ExtractionBackendError,\n    MissingDependencyError,\n)\n</code></pre> <p><code>ConfigurationError</code> is also exported from <code>memotrix</code>.</p> Exception Typical cause <code>ConfigurationError</code> Missing model, DSN, empty <code>add_text</code>, bad backend, bad store object <code>UnsupportedDocumentTypeError</code> Unknown extension, generic zip, <code>.xls</code> <code>EmptyDocumentError</code> Extractor produced no content <code>MissingDependencyError</code> Optional extra not installed (e.g. HEIC) <code>ExtractionBackendError</code> Parser backend failed <p>Also: <code>FileNotFoundError</code> / <code>ValueError</code> from extractors when the path is missing or not a file.</p>"},{"location":"api/extractors/","title":"Extractors API","text":"<pre><code>from memotrix.filetypes import extract_file, supported_extensions, register_extractor, ExtractorRegistry\n</code></pre> <ul> <li><code>extract_file(path, *, describe_images=True, generate_srt=False)</code> \u2014 same router <code>Memory.add</code> uses by default.</li> <li><code>supported_extensions()</code> \u2014 sorted list of wired + registered suffixes.</li> <li><code>register_extractor(extension, fn_or_instance)</code> \u2014 plugin registry.</li> </ul> <p><code>DocumentData</code> fields: <code>metadata</code>, <code>sections</code>, <code>tables</code>, <code>images</code>, <code>text</code>, <code>raw_text</code>, <code>language</code>, <code>summary</code>, <code>transcripts</code>, <code>validation</code>.</p> <p>Helper: <code>memotrix.utils.outputSturcture.build_document</code>.</p>"},{"location":"api/memory/","title":"<code>Memory</code>","text":"<pre><code>from memotrix import Memory\n</code></pre> <p>Implementation: <code>src/memotrix/memory.py</code>.</p>"},{"location":"api/memory/#constructor","title":"Constructor","text":"<pre><code>Memory(\n    embeddings: Embeddings | str,\n    *,\n    store=None,\n    connection: str | None = None,\n    backend: str | None = None,\n    config: MemoryConfig | None = None,\n    document_store: DocumentStore | None = None,\n    extract_file=None,\n)\n</code></pre> <ul> <li><code>store</code> \u2014 <code>InMemoryStore</code>, <code>PostgresStore</code>, <code>VectorStoreBundle</code>, or any object with <code>.dense</code> and <code>.sparse</code>.</li> <li><code>backend</code> \u2014 <code>\"memory\"</code> or <code>\"postgres\"</code>. If <code>connection</code> is set and backend is omitted, Postgres is selected.</li> <li><code>connection</code> with <code>backend=\"memory\"</code> raises <code>ConfigurationError</code>.</li> </ul>"},{"location":"api/memory/#class-methods","title":"Class methods","text":"<ul> <li><code>Memory.from_config(config, embeddings=None)</code> \u2014 requires <code>config.embedding_model</code> if <code>embeddings</code> is omitted.</li> <li><code>Memory.from_env()</code> \u2014 <code>MemoryConfig.from_env()</code>.</li> </ul>"},{"location":"api/memory/#addpath-describe_imagesnone-generate_srtfalse-dict","title":"<code>add(path, *, describe_images=None, generate_srt=False) -&gt; dict</code>","text":"<p>Extract + ingest. See file ingestion.</p>"},{"location":"api/memory/#add_texttext-source_idnone-memory_typesemantic-session_idnone-metadatanone-dict","title":"<code>add_text(text, *, source_id=None, memory_type=\"semantic\", session_id=None, metadata=None) -&gt; dict</code>","text":"<p>Empty/whitespace text raises <code>ConfigurationError</code>.</p>"},{"location":"api/memory/#add_documentsitems-dict","title":"<code>add_documents(items) -&gt; dict</code>","text":"<p><code>items</code> is a sequence of <code>DocumentData</code> or <code>(DocumentData, source_path)</code> tuples. Bypasses file extractors.</p>"},{"location":"api/memory/#searchquery-top_knone-max_tokens_returnednone-filtersnone-session_idnone-memory_typenone-expand_modenone-listdict","title":"<code>search(query, *, top_k=None, max_tokens_returned=None, filters=None, session_id=None, memory_type=None, expand_mode=None) -&gt; list[dict]</code>","text":"<p>Returns hit dictionaries. Common keys (when present):</p> Key Meaning <code>chunk_text</code> / <code>content</code> Text after expansion <code>matched_chunk</code> Pre-expansion match <code>score</code> Rank score (RRF or rerank \u00d7 memory multiplier) <code>chunk_id</code> Internal id <code>source_path</code> / <code>filename</code> Source <code>memory_type</code> / <code>session_id</code> Payload filters <code>expansion</code> <code>neighbors</code> / <code>full_file</code> / <code>none</code> <code>tokens_estimate</code> Approximate tokens <code>reranked</code> Whether cross-encoder ran <code>access_count</code> / <code>last_accessed</code> If access tracking is on"},{"location":"api/memory/#deletesource_path-int","title":"<code>delete(source_path) -&gt; int</code>","text":"<p>Deletes from dense, sparse, and document store. Empty string returns <code>0</code>. Return value is <code>max(dense_deleted, sparse_deleted)</code>.</p>"},{"location":"api/memory/#list_sources-listdict","title":"<code>list_sources() -&gt; list[dict]</code>","text":"<p>Unique <code>source_path</code> / <code>filename</code> with <code>chunks</code> counts from dense payloads.</p>"},{"location":"api/memory/#extractpath","title":"<code>extract(path, ...)</code>","text":"<p>Returns <code>DocumentData</code>.</p>"},{"location":"api/memory/#as_retriever-as_stack-close","title":"<code>as_retriever()</code> / <code>as_stack()</code> / <code>close()</code>","text":"<p><code>close()</code> closes the Postgres pool when using <code>PostgresStore</code>; no-op for in-memory native store.</p>"},{"location":"api/vectorstores/","title":"Vector stores","text":"<pre><code>from memotrix.vectorstores import InMemoryStore, PostgresStore, VectorStoreBundle\n</code></pre>"},{"location":"api/vectorstores/#inmemorystoreembeddings-max_elements50_000-spacecosine","title":"<code>InMemoryStore(embeddings, *, max_elements=50_000, space=\"cosine\")</code>","text":"<p>HNSW (<code>hnswlib</code>) + BM25 (<code>rank-bm25</code>). <code>max_elements</code> cannot grow after init. Data is process-local.</p>"},{"location":"api/vectorstores/#postgresstoreconnection-embeddings-table_namememotrix_chunks","title":"<code>PostgresStore(connection, embeddings, *, table_name=\"memotrix_chunks\")</code>","text":"<p>Empty <code>connection</code> raises <code>ConfigurationError</code>. <code>from_conn_string</code> is an alias constructor.</p>"},{"location":"api/vectorstores/#custom-stores","title":"Custom stores","text":"<p>Any object with <code>.dense</code> and <code>.sparse</code> (and optional <code>.native</code>, <code>.bundle</code>, <code>.close</code>) can be passed as <code>store=</code>. <code>build_retrieval_stack(..., custom_dense=, custom_sparse=)</code> wraps that pattern.</p>"},{"location":"api/vectorstores/#build_retrieval_stack","title":"<code>build_retrieval_stack</code>","text":"<pre><code>from memotrix import build_retrieval_stack\n\nstack = build_retrieval_stack(\n    backend=\"memory\",\n    embeddings=embeddings,\n)\n# stack[\"memory\"], stack[\"retrieval\"], stack[\"dense\"], ...\n</code></pre> <p>Requires embeddings or <code>embedding_model</code>. Used when you want the internal dict instead of only the facade.</p>"},{"location":"api-reference/memory-facade/","title":"API Reference: The <code>Memory</code> Facade","text":"<p>Canonical website page: Memory API. This file is kept for older links.</p> <p>The <code>Memory</code> class is the primary entry point for the Memotrix SDK. It acts as a facade, orchestrating the extraction, ingestion, vector stores, and retrieval pipelines.</p> <p>The <code>Memory</code> class is the primary entry point for the Memotrix SDK. It acts as a facade, orchestrating the extraction, ingestion, vector stores, and retrieval pipelines.</p> <pre><code>from memotrix import Memory\n</code></pre>"},{"location":"api-reference/memory-facade/#initialization","title":"Initialization","text":"<pre><code>Memory(\n    embeddings: Embeddings, \n    store: VectorStoreBundle = None,\n    backend: str = \"memory\",\n    connection: str = None,\n    extract_file: Callable = None\n)\n</code></pre> <p>Parameters: - <code>embeddings</code> (Embeddings): An instance of <code>Embeddings</code> (e.g. <code>HuggingFaceEmbeddings</code>). You can also pass a model string, which will be coerced into a HuggingFace model. - <code>store</code> (VectorStoreBundle): Optional. A pre-configured vector store (like <code>PostgresStore</code>). - <code>backend</code> (str): <code>\"memory\"</code> or <code>\"postgres\"</code>. Defaults to <code>\"memory\"</code>. - <code>connection</code> (str): Optional. A database connection string. If provided, <code>backend</code> is forced to <code>\"postgres\"</code>. - <code>extract_file</code> (Callable): Optional. A custom router function to bypass the default file extractors.</p>"},{"location":"api-reference/memory-facade/#alternate-constructors","title":"Alternate Constructors","text":"<ul> <li><code>Memory.from_config(config: MemoryConfig, embeddings=None)</code>: Instantiates from a configuration object.</li> <li><code>Memory.from_env(embeddings=None)</code>: Instantiates entirely using environment variables (e.g., <code>DATABASE_URL</code>, <code>EMBEDDING_MODEL</code>).</li> </ul>"},{"location":"api-reference/memory-facade/#methods","title":"Methods","text":""},{"location":"api-reference/memory-facade/#addpath-str-path-kwargs-dict","title":"<code>add(path: str | Path, **kwargs) -&gt; dict</code>","text":"<p>Ingests a file into memory. The file is extracted, chunked, embedded, and inserted into both dense and sparse indexes.</p> <p>Arguments: - <code>path</code>: The absolute or relative path to the file. - <code>describe_images</code> (bool): Defaults to True. If False, vision captioning for images is skipped. - <code>generate_srt</code> (bool): Defaults to False. If True, audio/video extractors will dump an SRT file alongside the source.</p> <p>Returns: A dictionary containing ingestion statistics: <code>{\"filename\": str, \"source_path\": str, \"chunks\": int, \"inserted\": int, \"merged\": int, \"extracted_images\": int}</code></p>"},{"location":"api-reference/memory-facade/#add_texttext-str-kwargs-dict","title":"<code>add_text(text: str, **kwargs) -&gt; dict</code>","text":"<p>Ingests raw text directly into memory without a file.</p> <p>Arguments: - <code>text</code>: The string to ingest. - <code>source_id</code>: A unique identifier for this text. If omitted, a UUID is generated. - <code>memory_type</code>: Defaults to <code>\"semantic\"</code>. Can be set to <code>\"episodic\"</code> or <code>\"procedural\"</code>. - <code>session_id</code>: Optional string to tag the text to a specific conversation or session. - <code>metadata</code>: Optional dictionary of extra JSON fields to store in the payload.</p>"},{"location":"api-reference/memory-facade/#add_documentsdocuments-list-kwargs-dict","title":"<code>add_documents(documents: List, **kwargs) -&gt; dict</code>","text":"<p>Ingests a list of pre-extracted <code>DocumentData</code> objects. This bypasses the extraction step entirely.</p>"},{"location":"api-reference/memory-facade/#searchquery-str-kwargs-listdict","title":"<code>search(query: str, **kwargs) -&gt; List[dict]</code>","text":"<p>Performs a hybrid search against the memory using the provided query string.</p> <p>Arguments: - <code>query</code>: The search string. - <code>top_k</code> (int): Number of results to return. Defaults to <code>RetrievalConfig.top_k</code> (3). - <code>max_tokens_returned</code> (int): A soft limit on the total size of the expanded chunks. Defaults to 800. - <code>filters</code> (dict): A dictionary of exact-match filters (e.g. <code>{\"department\": \"DevOps\"}</code>). - <code>session_id</code> (str): Convenience argument that is merged into <code>filters</code>. - <code>memory_type</code> (str): Convenience argument that is merged into <code>filters</code>. - <code>expand_mode</code> (str): <code>\"neighbors\"</code>, <code>\"full_file\"</code>, or <code>\"none\"</code>.</p> <p>Returns: A list of dictionaries. Each dictionary represents a Hit, containing the <code>chunk_text</code>, <code>score</code>, and payload metadata.</p>"},{"location":"api-reference/memory-facade/#deletesource_ref-str-int","title":"<code>delete(source_ref: str) -&gt; int</code>","text":"<p>Deletes all chunks, vectors, and DocumentStore adjacencies associated with a given source.</p> <p>Arguments: - <code>source_ref</code>: Can be the absolute <code>source_path</code>, the basename <code>filename</code>, or the <code>source_id</code> generated by <code>add_text()</code>.</p> <p>Returns: The number of rows deleted from the index.</p>"},{"location":"api-reference/memory-facade/#extractpath-str-path-kwargs-documentdata","title":"<code>extract(path: str | Path, **kwargs) -&gt; DocumentData</code>","text":"<p>Extracts a file using the Memotrix extractors and returns the parsed <code>DocumentData</code> object without chunking or indexing it. Useful for debugging or custom ETL pipelines.</p>"},{"location":"api-reference/memory-facade/#close","title":"<code>close()</code>","text":"<p>Closes any active database connections (like Postgres pools). Should be called when the application shuts down. No-op for in-memory stores.</p>"},{"location":"api-reference/vectorstores/","title":"API Reference: Vector Stores","text":"<p>Canonical website page: ../api/vectorstores.md.</p> <p>Vector Stores manage the storage and retrieval of Dense (vector) embeddings, Sparse (keyword) weights, and raw metadata payloads.</p> <p>Vector Stores manage the storage and retrieval of Dense (vector) embeddings, Sparse (keyword) weights, and raw metadata payloads.</p> <p>Memotrix bundles these capabilities into a <code>VectorStoreBundle</code> interface, which exposes <code>.dense</code> and <code>.sparse</code> properties.</p>"},{"location":"api-reference/vectorstores/#inmemorystore","title":"<code>InMemoryStore</code>","text":"<p>The <code>InMemoryStore</code> uses the <code>hnswlib</code> C++ library for lightning-fast, in-process approximate nearest neighbors search (Dense). It uses a custom <code>rank-bm25</code> implementation for sparse keyword search.</p> <pre><code>from memotrix.vectorstores import InMemoryStore\n</code></pre>"},{"location":"api-reference/vectorstores/#initialization","title":"Initialization","text":"<pre><code>InMemoryStore(\n    embeddings: Embeddings,\n    max_elements: int = 50000,\n    space: str = \"cosine\"\n)\n</code></pre> <p>Parameters: - <code>embeddings</code>: Required. The active embeddings instance (determines the vector dimension). - <code>max_elements</code>: The maximum number of chunks the HNSW index can hold. It cannot be resized dynamically after initialization. Defaults to 50,000. - <code>space</code>: The distance metric. Defaults to <code>\"cosine\"</code>.</p> <p>Persistence: The <code>InMemoryStore</code> is entirely volatile. When the Python process exits, all data is lost. </p>"},{"location":"api-reference/vectorstores/#postgresstore","title":"<code>PostgresStore</code>","text":"<p>The <code>PostgresStore</code> provides persistent, ACID-compliant, scalable storage using PostgreSQL and the <code>pgvector</code> extension. </p> <pre><code>from memotrix.vectorstores import PostgresStore\n</code></pre>"},{"location":"api-reference/vectorstores/#initialization_1","title":"Initialization","text":"<pre><code>PostgresStore(\n    connection: str,\n    embeddings: Embeddings,\n    table_name: str = \"memotrix_chunks\"\n)\n</code></pre> <p>Parameters: - <code>connection</code>: Required. A valid PostgreSQL Data Source Name (DSN) string. (e.g. <code>postgresql://user:pass@localhost/db</code>). - <code>embeddings</code>: Required. The active embeddings instance. The <code>vector</code> column in Postgres will be sized exactly to <code>embeddings.dimension</code>. - <code>table_name</code>: The name of the table to create/use. Defaults to <code>\"memotrix_chunks\"</code>.</p>"},{"location":"api-reference/vectorstores/#schema","title":"Schema","text":"<p>Upon initialization, <code>PostgresStore</code> ensures the <code>pgvector</code> extension is installed and executes a <code>CREATE TABLE IF NOT EXISTS</code> statement.</p> <p>The schema looks roughly like this: <pre><code>CREATE TABLE IF NOT EXISTS memotrix_chunks (\n    id UUID PRIMARY KEY,\n    source_path TEXT,\n    chunk_index INTEGER,\n    chunk_text TEXT,\n    embedding vector({dim}),\n    metadata JSONB,\n    sparse_vector tsvector\n);\n</code></pre></p> <p>It also automatically builds an HNSW index on the <code>embedding</code> column and a GIN index on the <code>sparse_vector</code> column to ensure blazing fast retrieval even with millions of rows.</p>"},{"location":"api-reference/vectorstores/#from_conn_string","title":"<code>from_conn_string()</code>","text":"<p>Alternatively, you can initialize it using the classmethod: <pre><code>store = PostgresStore.from_conn_string(\n    \"postgresql://localhost/db\",\n    embeddings=my_embeddings\n)\n</code></pre></p>"},{"location":"concepts/architecture/","title":"Architecture","text":"<p>Public usage lives in this <code>docs/</code> tree. This page is the website-facing architecture (merged from <code>docBook/07-system-architecture.md</code>).</p>"},{"location":"concepts/architecture/#high-level","title":"High level","text":"<pre><code>flowchart TD\n    User[\"Agent / your app\"] --&gt; Memory[\"Memory facade\"]\n    Memory --&gt; Extract[\"filetypes.extract_file\"]\n    Memory --&gt; Emb[\"Embeddings\"]\n    Memory --&gt; Store[\"InMemoryStore or PostgresStore\"]\n    Extract --&gt; Doc[\"DocumentData\"]\n    Doc --&gt; Ingest[\"IngestionPipeline\"]\n    Emb --&gt; Ingest\n    Ingest --&gt; Dense[\"Dense index\"]\n    Ingest --&gt; Sparse[\"Sparse index\"]\n    Ingest --&gt; DS[\"DocumentStore adjacency\"]\n    User --&gt; Search[\"RetrievalPipeline\"]\n    Search --&gt; Hybrid[\"HybridSearchEngine RRF\"]\n    Hybrid --&gt; Dense\n    Hybrid --&gt; Sparse\n    Search --&gt; DS</code></pre>"},{"location":"concepts/architecture/#packages","title":"Packages","text":"Package Role <code>memotrix.memory</code> <code>Memory</code> facade: <code>add</code>, <code>add_text</code>, <code>search</code>, <code>delete</code>, <code>list_sources</code> <code>memotrix.api</code> <code>build_retrieval_stack</code>, re-exports <code>memotrix.embeddings</code> <code>Embeddings</code> ABC, HuggingFace / OpenAI / Fake <code>memotrix.vectorstores</code> <code>InMemoryStore</code>, <code>PostgresStore</code>, <code>VectorStoreBundle</code> <code>memotrix.config</code> <code>MemoryConfig</code>, env / DSN resolution <code>memotrix.filetypes</code> Extractors + <code>ExtractorRegistry</code> <code>memotrix.processes</code> Ingestion, retrieval, document store, chunking, memory scoring <code>memotrix.vectorDB</code> HNSW, BM25, Postgres indexes, hybrid RRF <code>memotrix.utils</code> <code>DocumentData</code>, exceptions, optional AI providers"},{"location":"concepts/architecture/#stores","title":"Stores","text":"<p>In-memory: <code>HNSWDenseIndex</code> + <code>BM25SparseIndex</code>. Volatile. <code>max_elements</code> is fixed at index creation.</p> <p>Postgres: one table (default <code>memotrix_chunks</code>) with <code>vector(dim)</code> + <code>tsvector</code>. <code>dim</code> is <code>embeddings.dimension</code>. Changing embedding width requires a new table (or drop/recreate).</p>"},{"location":"concepts/architecture/#there-is-no-http-api-in-this-package","title":"There is no HTTP API in this package","text":"<p>Memotrix 0.2.0 on PyPI is a library. HTTP routes, if any, belong to a separate app that uses this package. Do not document REST endpoints that are not in <code>src/memotrix</code>.</p>"},{"location":"concepts/architecture/#related","title":"Related","text":"<ul> <li>System capabilities</li> <li>Core workflows</li> </ul>"},{"location":"concepts/chunking-and-expansion/","title":"Chunking and neighbor expansion","text":""},{"location":"concepts/chunking-and-expansion/#chunking","title":"Chunking","text":"<p>Ingestion splits <code>DocumentData</code> text into windows:</p> <ul> <li>Default 500 characters</li> <li>Default 50 character overlap</li> </ul> <p>Configured via <code>MemoryConfig.chunking</code> (<code>ChunkingConfig</code>). Overlap exists so a sentence split across a boundary still appears in two chunks.</p> <p>Programming files are pre-split by class/function regexes in <code>ProgrammingFileExtractor</code> before this windowing. CSV files are grouped in batches of 8 rows. Logs are grouped in windows of 40 non-empty lines.</p>"},{"location":"concepts/chunking-and-expansion/#documentstore","title":"DocumentStore","text":"<p>On ingest, Memotrix records chunk adjacency (<code>DocumentStore</code>). Retrieval can expand a hit to neighboring chunks in the same source.</p>"},{"location":"concepts/chunking-and-expansion/#expand-modes","title":"Expand modes","text":"<p>Passed on <code>search(..., expand_mode=...)</code> or <code>RetrievalConfig.expand_mode</code>.</p> Mode Behavior <code>neighbors</code> (default) Matched chunk \u00b1 <code>neighbor_window</code> (default 1), truncated by <code>max_tokens_returned</code> (default 800, ~4 chars/token) <code>full_file</code> Unique sources expanded to full reconstructed text (DocumentStore \u2192 all indexed chunks \u2192 limited disk re-read for some types) <code>none</code> Matched chunk text only <p>Hit dicts include <code>chunk_text</code>, <code>content</code>, <code>matched_chunk</code>, <code>expansion</code>, <code>tokens_estimate</code>, and may include <code>truncated</code>, <code>neighbor_chunk_indices</code>.</p> <p>Use <code>full_file</code> only when the source is small enough for the LLM context window.</p>"},{"location":"concepts/hybrid-search/","title":"Hybrid search","text":"<p>Every <code>memory.search()</code> query hits two indexes, then fuses ranks.</p> <ol> <li>Dense \u2014 cosine / HNSW or pgvector. Matches paraphrases (\u201cpuppy\u201d \u2248 \u201cdog\u201d).</li> <li>Sparse \u2014 BM25 in-process, or Postgres <code>tsvector</code>. Matches identifiers (<code>RX-78-2</code>, error codes).</li> </ol> <p>Fusion is Reciprocal Rank Fusion:</p> <pre><code>score(d) = \u03a3  1 / (fusion_k + rank(d))\n</code></pre> <p>Default <code>fusion_k=60</code> (<code>RetrievalConfig.fusion_k</code>). Implemented in <code>memotrix.vectorDB.hybrid.reciprocal_rank_fusion</code>.</p>"},{"location":"concepts/hybrid-search/#query-path","title":"Query path","text":"<pre><code>sequenceDiagram\n    participant App\n    participant Retrieval as RetrievalPipeline\n    participant Emb as Embeddings\n    participant Hybrid as HybridSearchEngine\n    App-&gt;&gt;Retrieval: search(query, top_k, filters)\n    Retrieval-&gt;&gt;Emb: embed_query (cached)\n    Retrieval-&gt;&gt;Hybrid: search_with_channels\n    Hybrid--&gt;&gt;Retrieval: fused + dense hits + sparse hits\n    alt Channels agree clearly\n        Retrieval--&gt;&gt;App: skip cross-encoder\n    else Reranker configured\n        Retrieval-&gt;&gt;Retrieval: CrossEncoder.predict\n    end\n    Retrieval-&gt;&gt;Retrieval: memory_score_multiplier\n    Retrieval-&gt;&gt;Retrieval: expand neighbors / full_file / none\n    Retrieval--&gt;&gt;App: list of hit dicts</code></pre>"},{"location":"concepts/hybrid-search/#conditional-rerank","title":"Conditional rerank","text":"<p>If <code>reranker_model</code> is set and <code>enable_rerank</code> is true, a cross-encoder may rescore candidates. Rerank is skipped when the fused ranking already has a large margin, or dense and sparse agree on top-1 (<code>RetrievalPipeline._should_skip_rerank</code>).</p> <p>Without a reranker model, scores are RRF (optionally multiplied by the memory boost).</p>"},{"location":"concepts/hybrid-search/#memory-boost","title":"Memory boost","text":"<p><code>memory_score_multiplier</code> can raise scores for recent, frequently accessed, or \u201cimportant\u201d (dedup-merged) chunks. Disable with <code>RetrievalConfig.enable_memory_boost=False</code>.</p>"},{"location":"concepts/hybrid-search/#query-embedding-cache","title":"Query embedding cache","text":"<p>Repeated agent queries reuse vectors (<code>query_cache_size</code>, default 256).</p>"},{"location":"concepts/hybrid-search/#tuning","title":"Tuning","text":"<p>See the how-to originally in <code>docs/how-to/hybrid-search-tuning.md</code> \u2014 the same knobs:</p> <pre><code>from memotrix import Memory, MemoryConfig\nfrom memotrix.config import RetrievalConfig\n\nconfig = MemoryConfig(\n    retrieval=RetrievalConfig(\n        top_k=5,\n        fusion_k=60,\n        enable_memory_boost=True,\n        enable_rerank=True,\n        reranker_model=\"cross-encoder/ms-marco-MiniLM-L-6-v2\",\n    )\n)\n</code></pre> <p>Rerankers add latency. Only set <code>reranker_model</code> if you measured that you need it.</p>"},{"location":"concepts/memory-types/","title":"Memory types (semantic, episodic, procedural)","text":"<p>Memotrix does not run three separate databases. <code>memory_type</code> is a string on the chunk payload. Default for <code>add()</code> and <code>add_text()</code> is <code>\"semantic\"</code>. Search can exact-match filter on it.</p> <p>The library does not forbid other strings. The three names below are the convention used in this project and in agent demos.</p>"},{"location":"concepts/memory-types/#semantic","title":"Semantic","text":"<p>Facts and world knowledge: manuals, policies, product copy.</p> <pre><code>memory.add(\"handbook.pdf\")  # metadata memory_type defaults to semantic\nmemory.add_text(\"Q3 revenue was 42 million.\", memory_type=\"semantic\")\n</code></pre>"},{"location":"concepts/memory-types/#episodic","title":"Episodic","text":"<p>Events and history: chat turns, tool results, \u201cwhat happened in this session\u201d.</p> <pre><code>memory.add_text(\n    \"User said: make the button blue.\",\n    memory_type=\"episodic\",\n    session_id=\"session_123\",\n)\nhits = memory.search(\n    \"what color did the user want?\",\n    memory_type=\"episodic\",\n    session_id=\"session_123\",\n)\n</code></pre> <p><code>session_id</code> is also an exact-match payload field.</p>"},{"location":"concepts/memory-types/#procedural","title":"Procedural","text":"<p>How-to instructions for the agent (deploy steps, citation rules).</p> <pre><code>memory.add_text(\"Always cite source_path in answers.\", memory_type=\"procedural\")\nhits = memory.search(\"how should answers be cited?\", memory_type=\"procedural\")\n</code></pre>"},{"location":"concepts/memory-types/#filters","title":"Filters","text":"<p><code>search(..., filters={\"department\": \"DevOps\"})</code> exact-matches extra metadata you passed to <code>add_text(..., metadata={...})</code>. Combined with <code>session_id</code> / <code>memory_type</code> kwargs.</p> <p>There is no partial-match or range filter in the public API.</p>"},{"location":"concepts/overview/","title":"How Memotrix works","text":""},{"location":"concepts/overview/#simple-explanation","title":"Simple explanation","text":"<p>You give Memotrix files or notes. It breaks them into small pieces, remembers the meaning of each piece (vectors) and the words in each piece (keyword index). When you ask a question, it finds the best few pieces and hands them to you. You (or your LLM) write the answer.</p>"},{"location":"concepts/overview/#technical-explanation","title":"Technical explanation","text":"<ol> <li>Extract \u2014 <code>extract_file</code> routes by extension (and JSON sniffing for FHIR / GeoJSON / chat / JSON-LD).</li> <li>Normalize \u2014 extractors return <code>DocumentData</code> (<code>metadata</code>, <code>sections</code>, <code>text</code>, optional <code>tables</code> / <code>images</code>).</li> <li>Chunk \u2014 <code>IngestionPipeline</code> splits text (<code>chunk_size</code> / <code>chunk_overlap</code>).</li> <li>Embed \u2014 <code>Embeddings.embed_documents</code> produces dense vectors. Dimension is taken from the model, never hardcoded.</li> <li>Index \u2014 dense (HNSW or pgvector) + sparse (BM25 or Postgres <code>tsvector</code>).</li> <li>Retrieve \u2014 embed the query, search both indexes, fuse with Reciprocal Rank Fusion, optional cross-encoder rerank, memory-score boost, neighbor expansion.</li> </ol> <pre><code>flowchart TD\n    A[File or add_text] --&gt; B[Extract / DocumentData]\n    B --&gt; C[Chunk]\n    C --&gt; D[Embed]\n    D --&gt; E[Dense index]\n    C --&gt; F[Sparse index]\n    Q[Query] --&gt; G[Embed query]\n    G --&gt; E\n    Q --&gt; F\n    E --&gt; H[RRF fusion]\n    F --&gt; H\n    H --&gt; I[Optional rerank]\n    I --&gt; J[Memory boost]\n    J --&gt; K[Neighbor expansion]\n    K --&gt; L[Hit list for your LLM]</code></pre>"},{"location":"concepts/overview/#internal-working","title":"Internal working","text":"<p>The <code>Memory</code> class (<code>src/memotrix/memory.py</code>) is a facade. It wires:</p> <ul> <li><code>IngestionPipeline</code> \u2014 chunk, dedup, write indexes, populate <code>DocumentStore</code></li> <li><code>HybridSearchEngine</code> \u2014 dense + sparse + RRF</li> <li><code>RetrievalPipeline</code> \u2014 query cache, conditional rerank, expansion, access tracking</li> </ul> <p><code>as_stack()</code> returns the same internals as a dict for embedding into a larger server.</p>"},{"location":"concepts/overview/#why-it-matters","title":"Why it matters","text":"<p>Agent loops cannot dump entire documents into the prompt. Default <code>top_k=3</code> and neighbor windows keep context small while still recovering split sentences via overlap + expansion.</p>"},{"location":"concepts/overview/#related","title":"Related","text":"<ul> <li>Architecture</li> <li>Hybrid search</li> <li>Chunking</li> </ul>"},{"location":"guides/demo/","title":"Live demos","text":"<p>Full copy-paste scripts live in DemoCodeLive. They import PyPI <code>memotrix</code>, not this repo\u2019s <code>src/</code>.</p> <pre><code>pip install memotrix\npip install \"memotrix[local]\"   # required for InMemoryStore\npip install pillow              # required to import Memory in 0.2.0\n</code></pre> <p>See run notes for an actual install attempt and failures recorded on Windows without MSVC.</p> <p>Map of scripts to use cases:</p> Demo Use case page 01 Agent memory 02 Document Q&amp;A 03\u201304 Memory types 05 Chunking 07 Custom extractors 09 RAG agent 10 File ingestion 11 Postgres"},{"location":"guides/deployment/","title":"Deployment","text":"<p>Memotrix is a library you vendor into your process (API worker, agent job, notebook).</p>"},{"location":"guides/deployment/#typical-layouts","title":"Typical layouts","text":"<pre><code>flowchart LR\n    App[Your agent process] --&gt; Mem[Memory]\n    Mem --&gt; PG[(PostgreSQL pgvector)]\n    Mem --&gt; HF[Local Sentence-Transformers]</code></pre> <p>Or fully in-process for a single worker (no Postgres): HNSW lives in RAM; do not run multiple processes that each think they own the same memories unless you use Postgres.</p>"},{"location":"guides/deployment/#checklist","title":"Checklist","text":"<ol> <li>Python 3.10+.</li> <li><code>pip install \"memotrix[postgres,memory,extractors]\"</code> (or a smaller extra set).</li> <li>Set <code>EMBEDDING_MODEL</code> and <code>DATABASE_URL</code> in the environment \u2014 not in git.</li> <li>Pin the embedding model. Changing dimension requires a new Postgres table.</li> <li>Size <code>max_elements</code> for in-memory HNSW before ingest.</li> <li>Call <code>memory.close()</code> on shutdown when using Postgres.</li> <li>There is no Memotrix-specific reverse proxy or SSL \u2014 terminate TLS at your app or database.</li> </ol>"},{"location":"guides/deployment/#ci","title":"CI","text":"<p>Install extras needed by tests; <code>pytest</code> extra is <code>dev</code>. This package\u2019s tests live in the GitHub repo, not on PyPI.</p>"},{"location":"guides/deployment/#documentation-website","title":"Documentation website","text":"<p>MkDocs is published by GitHub Actions to the <code>gh-pages</code> branch. See GitHub Pages.</p>"},{"location":"guides/deployment/#rollback","title":"Rollback","text":"<p>Pin <code>memotrix==0.2.0</code> in your app. Standard pip/poetry pin; the library has no migration runner beyond <code>CREATE TABLE IF NOT EXISTS</code>.</p>"},{"location":"guides/extras/","title":"Optional extras","text":"<p>Copied from <code>pyproject.toml</code> extras. Install only what you use.</p> <pre><code>pip install memotrix\npip install \"memotrix[memory]\"\npip install \"memotrix[postgres,memory,extractors]\"\npip install \"memotrix[all]\"\n</code></pre> <p>See the table in Installation.</p> <p><code>extractors</code> is an alias of <code>all-extractors</code>. Granular extras: <code>pdf</code>, <code>docx</code>, <code>pptx</code>, <code>excel</code>, <code>images</code>, <code>html</code>, <code>knowledge</code>, <code>yaml</code>.</p>"},{"location":"guides/github-pages/","title":"GitHub Pages (documentation site)","text":"<p>The MkDocs site is built by <code>.github/workflows/docs.yml</code> and published to the <code>gh-pages</code> branch.</p> <p>Live URL (after Pages is pointed at that branch): https://matrixxboy.github.io/memotrix/</p>"},{"location":"guides/github-pages/#why-actionsdeploy-pages-returned-404","title":"Why <code>actions/deploy-pages</code> returned 404","text":"<pre><code>Failed to create deployment (status: 404)\nEnsure GitHub Pages has been enabled:\nhttps://github.com/Matrixxboy/memotrix/settings/pages\n</code></pre> <p><code>actions/deploy-pages@v4</code> talks to the Pages deployment API. That API returns 404 until Pages is turned on for the repo. The MkDocs build can succeed (you already got an artifact) while deploy still fails.</p> <p>The workflow now pushes <code>site/</code> to <code>gh-pages</code>. That does not use the Pages deployment API.</p>"},{"location":"guides/github-pages/#one-time-github-setting","title":"One-time GitHub setting","text":"<ol> <li>Wait for Deploy docs to finish green (it will create/update branch <code>gh-pages</code>).</li> <li>Open Settings \u2192 Pages.</li> <li>Build and deployment \u2192 Source: Deploy from a branch.</li> <li>Branch: <code>gh-pages</code> / folder <code>/ (root)</code>.</li> <li>Save.</li> </ol> <p>GitHub will serve https://matrixxboy.github.io/memotrix/ after a minute or two.</p> <p>If you prefer Source: GitHub Actions instead of a branch, you must enable that source before using <code>actions/deploy-pages</code>. This repo\u2019s workflow uses the branch method so the first push does not 404.</p>"},{"location":"guides/github-pages/#local-preview","title":"Local preview","text":"<pre><code>pip install -r requirements-docs.txt\nmkdocs serve\n</code></pre> <p>Open http://127.0.0.1:8000</p>"},{"location":"guides/security/","title":"Security","text":"<p>Memotrix is an embeddable library. Your host application owns authn/authz, multi-tenancy, and network exposure.</p>"},{"location":"guides/security/#findings-documentation-level","title":"Findings (documentation-level)","text":"Severity Topic Notes High Untrusted file parse PDF/Office/HTML parsers can be attack surface. Sandbox or scan uploads. High Prompt injection via retrieved text Search will return hostile instructions if they were ingested. Frame context in the LLM prompt. Medium Secrets in env <code>DATABASE_URL</code>, <code>OPENAI_API_KEY</code> must not be committed. Library refuses silent defaults. Medium Postgres Parameterized queries in the store path; still protect the DSN and DB roles. Medium No auth layer Anyone who can call <code>Memory</code> in-process can read/write that store. Low Logging Retrieval may log query previews (<code>utils.trace</code>). Do not log secrets. Informational CORS / rate limit Not applicable inside the library. Informational Dependency risk Optional extras pull native libs (hnswlib, PyMuPDF, Whisper). Keep them updated. <p>Do not treat this page as a penetration-test report. No dedicated security test suite is claimed.</p>"},{"location":"guides/security/#recommendations","title":"Recommendations","text":"<ul> <li>Isolate tenants with separate tables, databases, or payload filters you enforce in the application.</li> <li>Disable image description / external AI if you cannot send file bytes to a provider.</li> <li>Prefer <code>describe_images=False</code> when processing untrusted images without a vision policy.</li> </ul>"},{"location":"guides/troubleshooting/","title":"Troubleshooting","text":""},{"location":"guides/troubleshooting/#hnswlib-will-not-install-on-windows","title":"<code>hnswlib</code> will not install on Windows","text":"<p>Cause: <code>hnswlib</code> is built from source; Microsoft C++ Build Tools are missing. There is no official Windows binary wheel for 0.8.0.</p> <p>Fix: install Build Tools, or run the demos on Linux/macOS, or use WSL.</p> <p>Then: <code>pip install \"memotrix[local]\"</code>.</p>"},{"location":"guides/troubleshooting/#modulenotfounderror-pil-after-pip-install-memotrix","title":"<code>ModuleNotFoundError: PIL</code> after <code>pip install memotrix</code>","text":"<p>Cause: 0.2.0 imports Pillow from <code>memotrix.utils.ai_integration</code> even when you do not ingest images.</p> <p>Fix: <code>pip install pillow</code> or <code>pip install \"memotrix[images]\"</code>.</p>"},{"location":"guides/troubleshooting/#pypi-__version__","title":"PyPI <code>__version__</code>","text":"<p>Package 0.2.0 on PyPI still reports <code>memotrix.__version__ == \"0.1.0\"</code>. Source 0.2.1 syncs <code>__version__</code> with <code>pyproject.toml</code>. Until you publish 0.2.1, use <code>importlib.metadata.version(\"memotrix\")</code>.</p>"},{"location":"guides/troubleshooting/#modulenotfounderror-hnswlib-rank_bm25-sentence_transformers","title":"<code>ModuleNotFoundError: hnswlib</code> / <code>rank_bm25</code> / <code>sentence_transformers</code>","text":"<p>Cause: <code>pip install memotrix</code> without extras.</p> <p>Fix: <code>pip install \"memotrix[memory]\"</code> (or <code>[local]</code> if you only need FakeEmbeddings + HNSW). On Windows, see the hnswlib section above.</p> <p>Verify: <code>python -c \"from memotrix.vectorstores import InMemoryStore\"</code>.</p>"},{"location":"guides/troubleshooting/#importing-memotrix-uses-the-git-checkout-not-pypi","title":"Importing <code>memotrix</code> uses the git checkout, not PyPI","text":"<p>Cause: <code>PYTHONPATH</code> includes <code>src/</code> or you ran from an editable install.</p> <p>Fix: new venv as in DemoCodeLive; <code>print(memotrix.__file__)</code> should be under <code>site-packages</code>.</p>"},{"location":"guides/troubleshooting/#configurationerror-embedding_model-is-not-set","title":"<code>ConfigurationError: EMBEDDING_MODEL is not set</code>","text":"<p>Cause: <code>Memory.from_env()</code> without env.</p> <p>Fix: set <code>EMBEDDING_MODEL</code> or pass <code>embeddings=</code> explicitly.</p>"},{"location":"guides/troubleshooting/#configurationerror-postgres-is-not-configured","title":"<code>ConfigurationError: Postgres is not configured</code>","text":"<p>Cause: backend postgres without DSN.</p> <p>Fix: <code>DATABASE_URL</code> or <code>connection=</code>.</p>"},{"location":"guides/troubleshooting/#unsupporteddocumenttypeerror","title":"<code>UnsupportedDocumentTypeError</code>","text":"<p>Cause: unknown suffix, generic zip, or <code>.xls</code>.</p> <p>Fix: convert files; or <code>register_extractor</code>.</p>"},{"location":"guides/troubleshooting/#emptydocumenterror","title":"<code>EmptyDocumentError</code>","text":"<p>Cause: blank file.</p> <p>Fix: skip empty sources.</p>"},{"location":"guides/troubleshooting/#search-returns-empty-or-irrelevant-hits","title":"Search returns empty or irrelevant hits","text":"<p>Causes: <code>FakeEmbeddings</code>; filters that match nothing; empty index; <code>memory_type</code> / <code>session_id</code> mismatch (exact match).</p> <p>Fix: confirm <code>list_sources()</code>; drop filters; use a real embedding model.</p>"},{"location":"guides/troubleshooting/#postgres-dimension-errors-after-changing-models","title":"Postgres dimension errors after changing models","text":"<p>Cause: <code>vector(n)</code> column width is fixed.</p> <p>Fix: new <code>table_name</code> or drop table.</p>"},{"location":"guides/troubleshooting/#whisper-ffmpeg-failures","title":"Whisper / ffmpeg failures","text":"<p>Cause: missing <code>audio</code> extra or ffmpeg.</p> <p>Fix: install extra; <code>ffmpeg -version</code>.</p>"},{"location":"guides/troubleshooting/#hnsw-capacity","title":"HNSW capacity","text":"<p>Cause: ingesting more than <code>max_elements</code>.</p> <p>Fix: raise <code>MemoryConfig.max_elements</code> before creating the store (cannot resize later).</p>"},{"location":"how-to/custom-extractors/","title":"How to Add Custom File Extractors","text":"<p>Memotrix supports over 20 file formats out of the box, but you may need to parse a proprietary format (like <code>.myext</code> or an internal XML structure). </p> <p>With the Extractor Plugin Registry, you can register custom extractors dynamically without forking the SDK.</p>"},{"location":"how-to/custom-extractors/#1-create-a-custom-extractor","title":"1. Create a Custom Extractor","text":"<p>All extractors must return a <code>DocumentData</code> object from the <code>memotrix.utils.models</code> module.  We provide a helper function <code>build_document</code> to make this easy.</p> <pre><code>from pathlib import Path\nfrom memotrix.utils.outputSturcture import build_document\n\nclass MyCustomExtractor:\n    def extract(self, path: Path, **kwargs):\n        # 1. Read your custom file\n        content = path.read_text(encoding=\"utf-8\")\n\n        # 2. Parse the content\n        parsed_text = f\"Parsed custom file: {content.strip()}\"\n\n        # 3. Build and return the DocumentData\n        return build_document(\n            path=path,\n            text=parsed_text,\n            extra_metadata={\"file_type\": \"myext\", \"parser\": \"MyCustomExtractor\"}\n        )\n</code></pre> <p>You can also use a simple function instead of a class:</p> <pre><code>def extract_myext(path: Path, **kwargs):\n    text = path.read_text()\n    return build_document(path, text, extra_metadata={\"file_type\": \"myext\"})\n</code></pre>"},{"location":"how-to/custom-extractors/#2-register-the-extractor","title":"2. Register the Extractor","text":"<p>Use the <code>register_extractor</code> function from <code>memotrix.filetypes</code> to map your custom extension to your extractor.</p> <pre><code>from memotrix.filetypes import register_extractor\n\n# Register the class instance\nregister_extractor(\".myext\", MyCustomExtractor())\n\n# Or register the function\n# register_extractor(\".myext\", extract_myext)\n</code></pre>"},{"location":"how-to/custom-extractors/#3-use-memotrix-as-normal","title":"3. Use Memotrix as Normal","text":"<p>Once registered, Memotrix's <code>Memory.add()</code> and <code>Memory.extract()</code> functions will automatically route files with your custom extension to your logic.</p> <pre><code>from memotrix import Memory\nfrom memotrix.embeddings import FakeEmbeddings\n\nmemory = Memory(embeddings=FakeEmbeddings(dim=8), backend=\"memory\")\n\n# The custom extractor will be invoked automatically!\nmemory.add(\"data/custom_file.myext\")\n</code></pre>"},{"location":"how-to/custom-extractors/#fallbacks-and-overrides","title":"Fallbacks and Overrides","text":"<p>You can override built-in extractors by registering a custom one for an existing extension.</p> <pre><code># Override the default PDF extractor\nregister_extractor(\".pdf\", MyBetterPDFExtractor())\n</code></pre>"},{"location":"how-to/hybrid-search-tuning/","title":"How to Tune Hybrid Search","text":"<p>Memotrix performs Hybrid Search by default. This means every query is executed against two different indexes: 1. Dense Index (Vectors): Finds chunks with similar semantic meaning (e.g., \"puppy\" matches \"dog\"). 2. Sparse Index (Keywords): Finds chunks containing exact keyword matches (e.g., \"RX-78-2\" matches exactly \"RX-78-2\").</p> <p>Memotrix fuses these two sets of results using Reciprocal Rank Fusion (RRF).</p>"},{"location":"how-to/hybrid-search-tuning/#tuning-reciprocal-rank-fusion","title":"Tuning Reciprocal Rank Fusion","text":"<p>The <code>fusion_k</code> parameter controls the RRF formula: <code>Score = 1 / (fusion_k + rank)</code></p> <p>By default, Memotrix uses <code>fusion_k = 60</code>, which is the industry standard recommendation. However, you can tune this via the <code>RetrievalConfig</code>.</p> <pre><code>from memotrix import Memory, MemoryConfig\nfrom memotrix.config import RetrievalConfig\n\nconfig = MemoryConfig(\n    backend=\"memory\",\n    embedding_model=\"BAAI/bge-small-en-v1.5\",\n    retrieval=RetrievalConfig(\n        top_k=5, \n        fusion_k=100  # Give less weight to the absolute top rankers\n    )\n)\n\nmemory = Memory.from_config(config)\n</code></pre>"},{"location":"how-to/hybrid-search-tuning/#tuning-memory-boosts","title":"Tuning Memory Boosts","text":"<p>Memotrix applies a multiplier to the final RRF score based on how the memory is accessed. This is called the <code>memory_multiplier</code>.</p> <p>It boosts chunks that: 1. Were created recently. 2. Have been accessed frequently (<code>access_count</code>). 3. Have been flagged as important during duplicate merging (<code>importance</code>).</p> <p>If you want pure relevance scores without temporal or access bias, you can disable this:</p> <pre><code>config = MemoryConfig(\n    retrieval=RetrievalConfig(\n        enable_memory_boost=False\n    )\n)\n</code></pre>"},{"location":"how-to/hybrid-search-tuning/#adding-a-cross-encoder-reranker","title":"Adding a Cross-Encoder Reranker","text":"<p>RRF is a mathematical fusion technique. For the absolute highest precision, you should pass the fused results through a Cross-Encoder Reranker.  The reranker reads both the query and the chunk text and outputs a highly accurate relevance score.</p> <p>Memotrix supports this out of the box. Just provide a <code>reranker_model</code>:</p> <pre><code>config = MemoryConfig(\n    backend=\"memory\",\n    embedding_model=\"BAAI/bge-small-en-v1.5\",\n    retrieval=RetrievalConfig(\n        enable_rerank=True,\n        reranker_model=\"cross-encoder/ms-marco-MiniLM-L-6-v2\"\n    )\n)\nmemory = Memory.from_config(config)\n</code></pre> <p>[!TIP] Rerankers are extremely slow compared to vector search. Memotrix automatically skips the reranker if the top result from both the Dense and Sparse indexes agree, saving significant latency. This conditional logic is enabled by default.</p>"},{"location":"how-to/postgres-migration/","title":"How to Migrate to PostgreSQL (pgvector)","text":"<p>Memotrix uses an in-memory <code>hnswlib</code> dense index and a <code>rank-bm25</code> sparse index by default. This is perfect for prototyping, but all memory is wiped when your Python script exits.</p> <p>For persistent, scalable, and ACID-compliant storage, Memotrix natively supports PostgreSQL with the <code>pgvector</code> extension.</p>"},{"location":"how-to/postgres-migration/#1-prerequisites","title":"1. Prerequisites","text":"<p>You need the <code>postgres</code> optional dependency installed:</p> <pre><code>pip install \"memotrix[postgres]\"\n</code></pre> <p>You also need a PostgreSQL database with the <code>pgvector</code> extension installed. If you have Docker, you can run the provided <code>docker-compose.yml</code>:</p> <pre><code>cd memory/\ndocker compose up -d\n</code></pre>"},{"location":"how-to/postgres-migration/#2-initialize-memory-with-postgres","title":"2. Initialize Memory with Postgres","text":"<p>There are two ways to tell Memotrix to use PostgreSQL: using configuration variables or directly passing a connection string.</p>"},{"location":"how-to/postgres-migration/#method-a-environment-variables-recommended","title":"Method A: Environment Variables (Recommended)","text":"<p>Set the <code>DATABASE_URL</code> environment variable: <pre><code>export DATABASE_URL=\"postgresql://postgres:postgres@localhost:5432/memotrix\"\n</code></pre></p> <p>Initialize <code>Memory</code> and let it auto-detect the backend: <pre><code>import os\nfrom memotrix import Memory\nfrom memotrix.embeddings import HuggingFaceEmbeddings\n\nembeddings = HuggingFaceEmbeddings(model=\"BAAI/bge-small-en-v1.5\")\n\n# Memotrix will automatically use PostgresStore because connection= is provided\nmemory = Memory(\n    embeddings=embeddings, \n    connection=os.environ[\"DATABASE_URL\"]\n)\n</code></pre></p>"},{"location":"how-to/postgres-migration/#method-b-explicit-initialization","title":"Method B: Explicit Initialization","text":"<p>You can explicitly pass a <code>PostgresStore</code> instance. This is useful if you need to override the default table name (<code>memotrix_chunks</code>).</p> <pre><code>import os\nfrom memotrix import Memory\nfrom memotrix.embeddings import HuggingFaceEmbeddings\nfrom memotrix.vectorstores import PostgresStore\n\nembeddings = HuggingFaceEmbeddings(model=\"BAAI/bge-small-en-v1.5\")\n\nstore = PostgresStore(\n    connection=os.environ[\"DATABASE_URL\"],\n    embeddings=embeddings,\n    table_name=\"my_custom_chunks_table\"\n)\n\nmemory = Memory(embeddings=embeddings, store=store)\n</code></pre>"},{"location":"how-to/postgres-migration/#3-what-happens-under-the-hood","title":"3. What Happens Under the Hood?","text":"<p>When you initialize a <code>PostgresStore</code>: 1. Memotrix connects to Postgres and ensures the <code>pgvector</code> extension is enabled. 2. It creates the chunk table (if it doesn't exist) with a <code>vector</code> column sized exactly to your <code>embeddings.dimension</code>. 3. It creates an HNSW index on the vector column. 4. It creates a <code>tsvector</code> index for sparse keyword search (BM25 approximation).</p> <p>[!WARNING] If you change your embedding model to one with a different dimension (e.g., switching from <code>bge-small</code> [384] to <code>openai</code> [1536]), you MUST use a new <code>table_name</code> or drop the old table. Postgres cannot alter the dimension of an existing <code>vector</code> column without wiping the data.</p>"},{"location":"tutorials/01-quickstart/","title":"Tutorial 1: Quickstart (5 Minutes)","text":"<p>In this tutorial, you'll learn how to set up Memotrix and add your first document into memory.</p>"},{"location":"tutorials/01-quickstart/#prerequisites","title":"Prerequisites","text":"<p>Memotrix requires Python 3.10+.</p>"},{"location":"tutorials/01-quickstart/#1-installation","title":"1. Installation","text":"<p>Memotrix uses granular optional dependencies, so you only install what you need. For this quickstart, we will use the in-memory backend with local HuggingFace embeddings, and we will extract a standard PDF.</p> <pre><code>pip install memotrix\npip install \"memotrix[memory]\"\n# PDF extra only if you ingest PDFs:\npip install \"memotrix[memory,pdf]\"\n</code></pre> <p>Note: The quotes around the package name are necessary for zsh and some other shells.</p>"},{"location":"tutorials/01-quickstart/#2-initialize-memory","title":"2. Initialize Memory","text":"<p>To use Memotrix, you need to instantiate the <code>Memory</code> facade.  By default, this will use an in-memory HNSW index (dense vectors) and an in-memory BM25 index (keyword search).</p> <pre><code>from memotrix import Memory\nfrom memotrix.embeddings import HuggingFaceEmbeddings\n\n# 1. Choose an embedding model\n# We use BGE-small because it's extremely fast for local execution.\nembeddings = HuggingFaceEmbeddings(model=\"BAAI/bge-small-en-v1.5\")\n\n# 2. Initialize the Memory system\nmemory = Memory(embeddings=embeddings, backend=\"memory\")\n</code></pre>"},{"location":"tutorials/01-quickstart/#3-ingest-a-document","title":"3. Ingest a Document","text":"<p>Let's add a document to our memory.</p> <pre><code># Create a sample text file\nwith open(\"sample_notes.txt\", \"w\") as f:\n    f.write(\"Memotrix is a composable hybrid memory library for agents. It supports dense and sparse indexes.\")\n\n# Add the file to Memory\nstats = memory.add(\"sample_notes.txt\")\nprint(\"Ingestion stats:\", stats)\n# Output: {'chunks': 1, 'merged': 0, 'inserted': 1}\n</code></pre> <p>The <code>add</code> method automatically: 1. Detects the file type. 2. Extracts the raw text and metadata. 3. Chunks the text (default 500 chars). 4. Embeds the chunks. 5. Inserts them into both the Dense (vector) and Sparse (keyword) indexes.</p>"},{"location":"tutorials/01-quickstart/#4-search-the-memory","title":"4. Search the Memory","text":"<p>Now, we can query our memory. Memotrix uses Hybrid Search by default, meaning it combines semantic meaning (vectors) with exact keyword matching (BM25) and fuses the results using Reciprocal Rank Fusion (RRF).</p> <pre><code>results = memory.search(\"What is Memotrix?\", top_k=1)\n\nfor hit in results:\n    print(f\"Score: {hit['score']}\")\n    print(f\"Content: {hit['chunk_text']}\")\n</code></pre>"},{"location":"tutorials/01-quickstart/#5-add-direct-facts","title":"5. Add Direct Facts","text":"<p>You can also add free-text facts or chat history directly without a file.</p> <pre><code>memory.add_text(\n    \"The user's favorite programming language is Python.\",\n    memory_type=\"semantic\"\n)\n</code></pre>"},{"location":"tutorials/01-quickstart/#next-steps","title":"Next Steps","text":"<p>Congratulations! You've successfully initialized Memotrix, ingested a file, and retrieved information using hybrid search.</p> <p>Next, check out Tutorial 2: Build a RAG Agent to learn how to hook this memory up to an LLM.</p>"},{"location":"tutorials/02-build-a-rag-agent/","title":"Tutorial 2: Build a RAG Agent","text":"<p>In this tutorial, you will build a complete Retrieval-Augmented Generation (RAG) loop using Memotrix as the knowledge base and an LLM (like OpenAI) to generate answers based on the retrieved context.</p>"},{"location":"tutorials/02-build-a-rag-agent/#1-setup","title":"1. Setup","text":"<p>Install the required packages. We will install the <code>openai</code> extra for the LLM.</p> <pre><code>pip install \"memotrix[memory,openai]\"\n</code></pre> <p>Set your OpenAI API key in your terminal: <pre><code>export OPENAI_API_KEY=\"sk-...\"\n</code></pre></p>"},{"location":"tutorials/02-build-a-rag-agent/#2-initialize-memotrix","title":"2. Initialize Memotrix","text":"<p>We will use <code>OpenAIEmbeddings</code> for the vector search this time to keep everything within the OpenAI ecosystem.</p> <pre><code>import os\nfrom memotrix import Memory\nfrom memotrix.embeddings import OpenAIEmbeddings\n\n# Initialize embeddings\nembeddings = OpenAIEmbeddings(model=\"text-embedding-3-small\")\n\n# Initialize memory\nmemory = Memory(embeddings=embeddings, backend=\"memory\")\n</code></pre>"},{"location":"tutorials/02-build-a-rag-agent/#3-populate-knowledge","title":"3. Populate Knowledge","text":"<p>Let's add some specific procedural knowledge that the LLM wouldn't know otherwise.</p> <pre><code>memory.add_text(\n    \"To deploy the Alpha engine, you must run `alpha deploy --force`. Never deploy on Fridays.\",\n    memory_type=\"procedural\",\n    metadata={\"department\": \"DevOps\"}\n)\n\nmemory.add_text(\n    \"The Alpha engine requires Node.js v18.0 or higher.\",\n    memory_type=\"semantic\",\n    metadata={\"department\": \"DevOps\"}\n)\n</code></pre>"},{"location":"tutorials/02-build-a-rag-agent/#4-the-agent-loop","title":"4. The Agent Loop","text":"<p>A RAG agent follows three steps: Retrieve, Augment, and Generate.</p> <pre><code>from openai import OpenAI\n\nclient = OpenAI()\n\ndef ask_agent(question: str):\n    # 1. Retrieve: Search Memotrix for relevant context\n    hits = memory.search(\n        question, \n        top_k=2, \n        # We can optionally filter by metadata!\n        filters={\"department\": \"DevOps\"}\n    )\n\n    # 2. Augment: Format the context for the LLM\n    context_blocks = [hit.get(\"chunk_text\") for hit in hits if hit.get(\"chunk_text\")]\n    context_str = \"\\n\\n---\\n\\n\".join(context_blocks)\n\n    prompt = f\"\"\"You are a helpful DevOps assistant. Answer the user's question using ONLY the context provided below.\n\nContext:\n{context_str}\n\nUser Question: {question}\n\"\"\"\n\n    # 3. Generate: Call the LLM\n    response = client.chat.completions.create(\n        model=\"gpt-4o\",\n        messages=[{\"role\": \"user\", \"content\": prompt}]\n    )\n\n    return response.choices[0].message.content\n\n# Test the agent\nanswer = ask_agent(\"How do I deploy the Alpha engine, and are there any restrictions?\")\nprint(answer)\n</code></pre> <p>Expected Output:</p> <p>To deploy the Alpha engine, you must run the command <code>alpha deploy --force</code>. However, there is a restriction: you must never deploy on Fridays.</p>"},{"location":"tutorials/02-build-a-rag-agent/#conclusion","title":"Conclusion","text":"<p>You have just built a fully functional RAG agent. By passing <code>context</code> from <code>memory.search()</code> into your LLM prompt, you ground the model's responses in your private data and prevent hallucinations.</p> <p>For more advanced setups, look into: - Migrating to PostgreSQL for persistent storage. - Chunking and Neighbor Expansion to provide the LLM with surrounding context.</p>"},{"location":"use-cases/","title":"Use cases","text":"<p>Each page maps to a real API in <code>memotrix</code>. Live scripts: DemoCodeLive.</p>"},{"location":"use-cases/#agent-and-rag","title":"Agent and RAG","text":"Use case What you call Document Long-term facts, sessions, procedures <code>add_text</code> + <code>memory_type</code> / <code>session_id</code> Agent long-term memory Question answering over files <code>add</code> + <code>search</code> Document Q&amp;A Retrieve then prompt an LLM <code>search</code> \u2192 your chat API RAG agent Durable store across processes <code>PostgresStore</code> / <code>from_env</code> Postgres"},{"location":"use-cases/#ingestion","title":"Ingestion","text":"Use case What you call Document Any wired file type <code>memory.add(path)</code> File ingestion CSV / Excel / JSON / YAML / SQL / XML same Structured data Source code <code>.py</code> <code>.js</code> <code>.ts</code> <code>.java</code> <code>.go</code> <code>.rs</code> <code>.cpp</code> Codebase memory Images, audio, video extras + optional Whisper Images and media Email, chat, logs <code>.eml</code> <code>.mbox</code> chat JSON, WhatsApp <code>.txt</code>, <code>.log</code> Email, chat, logs FHIR, GeoJSON, RDF sniffed JSON or KG extensions Knowledge graphs and FHIR Proprietary format <code>register_extractor</code> or <code>extract_file=</code> Custom extractors"},{"location":"use-cases/#operations","title":"Operations","text":"Use case API Inspect what is stored <code>list_sources()</code> Remove one source <code>delete(source_path_or_id)</code> Parse without indexing <code>extract(path)</code> Ingest pre-parsed docs <code>add_documents(...)</code> Tune retrieval <code>MemoryConfig.retrieval</code> Tests without a real model <code>FakeEmbeddings(dim=...)</code>"},{"location":"use-cases/#what-memotrix-is-not","title":"What Memotrix is not","text":"<ul> <li>Not a chatbot. You own the LLM call.</li> <li>Not a multi-tenant SaaS. Isolation is your filters and your database.</li> <li>Not a REST server in this package.</li> <li>Not a crawler. You pass local paths.</li> </ul>"},{"location":"use-cases/agent-long-term-memory/","title":"Use case: agent long-term memory","text":"<p>Problem. An LLM forgets everything when the context window rolls. You need durable facts, session events, and standing instructions.</p> <p>What Memotrix does. <code>add_text</code> writes free-form memories into the same hybrid index as files. You tag <code>memory_type</code> and optional <code>session_id</code>.</p> <pre><code>from memotrix import Memory\nfrom memotrix.embeddings import FakeEmbeddings  # or HuggingFaceEmbeddings\n\nmemory = Memory(embeddings=FakeEmbeddings(dim=16), backend=\"memory\")\n\nmemory.add_text(\n    \"User prefers dark mode.\",\n    source_id=\"prefs\",\n    memory_type=\"semantic\",\n)\nmemory.add_text(\n    \"User asked to hide the billing tab in this chat.\",\n    memory_type=\"episodic\",\n    session_id=\"chat-42\",\n)\nmemory.add_text(\n    \"Always cite source_path when answering from memory.\",\n    memory_type=\"procedural\",\n    source_id=\"cite-rule\",\n)\n\nfacts = memory.search(\"preferred theme\", memory_type=\"semantic\")\nsession = memory.search(\"what should be hidden?\", memory_type=\"episodic\", session_id=\"chat-42\")\nrules = memory.search(\"how to cite\", memory_type=\"procedural\")\n\nmemory.close()\n</code></pre> <p>Inputs: non-empty string; optional <code>source_id</code> (else <code>text:&lt;uuid&gt;</code>); <code>memory_type</code>; <code>session_id</code>; extra <code>metadata</code>.</p> <p>Processing: wrapped as <code>DocumentData</code> with <code>source_path</code> = <code>source_id</code>, then the ingestion pipeline (chunk, embed, index, dedup).</p> <p>Output: same stats dict as <code>add</code> (<code>chunks</code>, <code>inserted</code>, <code>merged</code>, \u2026). Search returns hit dicts with <code>chunk_text</code> and payload fields.</p> <p>Limitations: filters are exact match only. In-memory store is lost when the process exits. <code>FakeEmbeddings</code> will not rank like a real model.</p> <p>See Memory types and demo <code>DemoCodeLive/demos/03_memory_types.py</code>.</p>"},{"location":"use-cases/codebase-memory/","title":"Use case: codebase memory","text":"<p><code>ProgrammingFileExtractor</code> reads source and splits on language-specific class/function patterns, then the ingest pipeline chunks those blocks.</p> <p>Supported: <code>.py</code> <code>.js</code> <code>.ts</code> <code>.java</code> <code>.cpp</code> <code>.cc</code> <code>.cxx</code> <code>.go</code> <code>.rs</code>.</p> <pre><code>memory.add(\"app/main.py\")\nhits = memory.search(\"where is hybrid search fused?\", memory_type=\"semantic\")\n</code></pre> <p>Metadata includes <code>file_type=source_code</code>, <code>language</code>, <code>block_count</code>.</p> <p>Limitations. Regex splitting is heuristic, not a full AST. Unsupported languages: use <code>register_extractor</code> or ingest as <code>.txt</code>.</p> <p>Demo: <code>DemoCodeLive/samples/sample_code.py</code> and <code>demos/10_file_types.py</code>.</p>"},{"location":"use-cases/custom-extractors/","title":"Use case: custom extractors","text":"<p>Two extension points exist in the shipped code.</p>"},{"location":"use-cases/custom-extractors/#1-registry-per-extension","title":"1. Registry (per extension)","text":"<pre><code>from pathlib import Path\nfrom memotrix.filetypes import register_extractor\nfrom memotrix.utils.outputSturcture import build_document\n\ndef extract_myext(path: Path, **kwargs):\n    text = path.read_text(encoding=\"utf-8\")\n    return build_document(path, text, extra_metadata={\"file_type\": \"myext\"})\n\nregister_extractor(\".myext\", extract_myext)\nmemory.add(\"data/file.myext\")\n</code></pre> <p>A class with <code>.extract(path)</code> also works. Registry is checked before built-in routers, so you can override <code>.pdf</code>.</p>"},{"location":"use-cases/custom-extractors/#2-constructor-router","title":"2. Constructor router","text":"<pre><code>memory = Memory(embeddings=embeddings, extract_file=my_full_router)\n</code></pre> <p><code>my_full_router</code> replaces all routing for that <code>Memory</code> instance. It must accept <code>describe_images</code> and <code>generate_srt</code> keyword arguments (see <code>Memory.extract</code>).</p> <p>Extractors must return <code>DocumentData</code> (<code>memotrix.utils.models</code>).</p> <p>Demo: <code>DemoCodeLive/demos/07_custom_extractor.py</code>.</p>"},{"location":"use-cases/document-question-answering/","title":"Use case: document question answering","text":"<p>Problem. You have reports, notes, or PDFs. You want answers grounded in those files, not the model\u2019s training data.</p> <p>What Memotrix does. <code>add(path)</code> extracts and indexes. <code>search(query)</code> returns the top chunks for you to put in a prompt.</p> <pre><code>memory.add(\"quarterly_notes.txt\")\nhits = memory.search(\"what is the revenue?\", top_k=3, max_tokens_returned=800)\ncontext = \"\\n\\n\".join(h.get(\"chunk_text\") or \"\" for h in hits)\n# pass `context` to your LLM\n</code></pre> <p>Filters. <code>memory_type=\"semantic\"</code> (default on files) or extra metadata if you set it via <code>add_documents</code>.</p> <p>Sources. <code>list_sources()</code> returns <code>{source_path, filename, chunks}</code> for each ingested path.</p> <p>Delete. <code>delete(resolved_path)</code> or filename / <code>source_id</code> as implemented by <code>delete_by_source_path</code> on both indexes.</p> <p>Limitations. Quality depends on the embedding model and chunk size. Neighbor expansion can still miss distant related sections. PDFs need <code>pip install \"memotrix[pdf]\"</code> (or <code>extractors</code>). Empty files raise <code>EmptyDocumentError</code>.</p> <p>Demos: <code>DemoCodeLive/demos/02_ingest_files.py</code>, <code>09_rag_loop.py</code>.</p>"},{"location":"use-cases/email-chat-logs/","title":"Use case: email, chat, and logs","text":""},{"location":"use-cases/email-chat-logs/#email","title":"Email","text":"<p><code>.eml</code> and <code>.mbox</code> \u2192 subject, from, to, date, body (HTML stripped when needed). Natural memory type for these texts is episodic, but <code>add()</code> still defaults <code>memory_type</code> to <code>semantic</code> unless you ingest via <code>add_documents</code> with your own metadata.</p>"},{"location":"use-cases/email-chat-logs/#chat-json-jsonl","title":"Chat JSON / JSONL","text":"<p>If JSON looks like a list of messages (<code>content</code>/<code>text</code> + author/time keys), <code>ChatExtractor</code> runs. Nested <code>messages</code> / <code>chats</code> / <code>history</code> are detected.</p>"},{"location":"use-cases/email-chat-logs/#whatsapp-export","title":"WhatsApp export","text":"<p>A <code>.txt</code> whose first lines match <code>[...] Name:</code> is treated as chat, not a generic text file.</p>"},{"location":"use-cases/email-chat-logs/#logs","title":"Logs","text":"<p><code>.log</code> files are split into windows of 40 non-empty lines, optionally headed with ISO timestamps if lines start with them.</p> <pre><code>memory.add(\"app.log\")\nhits = memory.search(\"connection refused\", expand_mode=\"none\")\n</code></pre> <p><code>expand_mode=\"none\"</code> is often better for logs: neighbors may be unrelated lines.</p> <p>Demo samples: <code>DemoCodeLive/samples/chat.json</code>, <code>sample.log</code>.</p>"},{"location":"use-cases/file-ingestion/","title":"Use case: file ingestion","text":"<p><code>Memory.add(path)</code> is the single entry for files. Routing is <code>memotrix.filetypes.extract_file</code>.</p>"},{"location":"use-cases/file-ingestion/#wired-extensions","title":"Wired extensions","text":"Group Extensions Documents <code>.pdf</code> <code>.docx</code> <code>.pptx</code> <code>.txt</code> <code>.md</code> <code>.markdown</code> <code>.html</code> <code>.htm</code> <code>.epub</code> Data <code>.csv</code> <code>.xlsx</code> <code>.json</code> <code>.sql</code> <code>.xml</code> <code>.yaml</code> <code>.yml</code> Images <code>.png</code> <code>.jpg</code> <code>.jpeg</code> <code>.webp</code> <code>.bmp</code> <code>.gif</code> <code>.tiff</code> <code>.tif</code> <code>.svg</code> <code>.heic</code> Video <code>.mp4</code> <code>.mkv</code> <code>.avi</code> <code>.mov</code> <code>.webm</code> <code>.m4v</code> <code>.wmv</code> Audio <code>.mp3</code> <code>.wav</code> <code>.aac</code> <code>.m4a</code> <code>.ogg</code> <code>.flac</code> <code>.wma</code> Code <code>.py</code> <code>.js</code> <code>.ts</code> <code>.java</code> <code>.cpp</code> <code>.cc</code> <code>.cxx</code> <code>.go</code> <code>.rs</code> Email <code>.eml</code> <code>.mbox</code> Geo <code>.geojson</code> (and GeoJSON sniffed from <code>.json</code>) Knowledge <code>.ttl</code> <code>.nt</code> <code>.nq</code> <code>.rdf</code> <code>.owl</code> <code>.trig</code> <code>.jsonld</code> <code>.graphml</code> Learning <code>.scorm</code> or <code>.zip</code> only if it contains <code>imsmanifest.xml</code> Other <code>.jsonl</code> <code>.log</code> <p><code>.txt</code> that looks like WhatsApp (<code>[timestamp] Name: ...</code>) is routed to the chat extractor. <code>.json</code> is sniffed: FHIR, GeoJSON, chat, JSON-LD graph, else generic JSON.</p>"},{"location":"use-cases/file-ingestion/#add-kwargs","title":"<code>add</code> kwargs","text":"<ul> <li><code>describe_images</code> \u2014 default from <code>IngestConfig.describe_images</code> (<code>True</code>). Set <code>False</code> to skip vision captions.</li> <li><code>generate_srt</code> \u2014 audio/video extractors may write an SRT next to the source when <code>True</code>.</li> </ul>"},{"location":"use-cases/file-ingestion/#return-value","title":"Return value","text":"<pre><code>{\n  \"filename\": \"notes.txt\",\n  \"path\": \"/abs/path/notes.txt\",\n  \"chunks\": 1,\n  \"inserted\": 1,\n  \"merged\": 0,\n  \"extracted_images\": 0,\n}\n</code></pre> <p>(<code>path</code> is the resolved filesystem path.)</p>"},{"location":"use-cases/file-ingestion/#extract-only","title":"Extract only","text":"<p><code>memory.extract(path)</code> returns <code>DocumentData</code> without indexing \u2014 useful to debug parsers.</p>"},{"location":"use-cases/file-ingestion/#unsupported","title":"Unsupported","text":"<ul> <li>Generic zip archives \u2192 <code>UnsupportedDocumentTypeError</code></li> <li><code>.xls</code> (BIFF) \u2192 convert to <code>.xlsx</code></li> </ul> <p>Demo: <code>DemoCodeLive/demos/10_file_types.py</code>.</p>"},{"location":"use-cases/images-and-media/","title":"Use case: images, audio, and video","text":""},{"location":"use-cases/images-and-media/#images","title":"Images","text":"<p>Requires <code>pip install \"memotrix[images]\"</code>. Extensions: PNG, JPEG, WebP, BMP, GIF, TIFF, SVG, HEIC.</p> <ul> <li><code>describe_images=True</code> (default): <code>describe_image</code> in <code>utils/ai_integration.py</code>. If no vision provider is configured, captions fall back to local/heuristic text, not a guaranteed cloud vision model.</li> <li><code>describe_images=False</code>: placeholder description (<code>Image file {name}</code>).</li> <li>SVG conversion uses <code>cairosvg</code> if installed; otherwise <code>MissingDependencyError</code>.</li> <li>HEIC uses <code>pillow-heif</code>.</li> </ul> <p>PDF/DOCX embedded images can be counted in <code>add</code>\u2019s <code>extracted_images</code>.</p>"},{"location":"use-cases/images-and-media/#audio","title":"Audio","text":"<p>Requires <code>pip install \"memotrix[audio]\"</code> and ffmpeg on PATH. <code>AudioExtractor</code> uses <code>faster-whisper</code> (default model size <code>\"base\"</code>), converting to 16 kHz mono WAV.</p> <p><code>memory.add(\"call.mp3\", generate_srt=True)</code> may write an SRT beside the file.</p>"},{"location":"use-cases/images-and-media/#video","title":"Video","text":"<p>Same Whisper + ffmpeg path: audio is stripped from the container, then transcribed. Extensions: mp4, mkv, avi, mov, webm, m4v, wmv.</p>"},{"location":"use-cases/images-and-media/#limitations","title":"Limitations","text":"<ul> <li>Transcription quality and latency depend on Whisper model size and CPU/GPU \u2014 not measured in this DocBook unless you run them.</li> <li>Video does not index visual frames as a separate embedding space; it indexes the transcript (and any summary the extractor attaches).</li> </ul>"},{"location":"use-cases/knowledge-graphs-and-fhir/","title":"Use case: knowledge graphs, GeoJSON, and FHIR","text":""},{"location":"use-cases/knowledge-graphs-and-fhir/#fhir","title":"FHIR","text":"<p><code>.json</code> with <code>resourceType</code> (string) is routed to <code>FHIRExtractor</code>.</p>"},{"location":"use-cases/knowledge-graphs-and-fhir/#geojson","title":"GeoJSON","text":"<p><code>.geojson</code> or JSON with <code>type</code> <code>Feature</code> / <code>FeatureCollection</code>.</p>"},{"location":"use-cases/knowledge-graphs-and-fhir/#knowledge-graphs","title":"Knowledge graphs","text":"<p>Extensions: <code>.ttl</code> <code>.nt</code> <code>.nq</code> <code>.rdf</code> <code>.owl</code> <code>.trig</code> <code>.jsonld</code> <code>.graphml</code>. JSON-LD (<code>@graph</code> or <code>@context</code>+<code>@id</code>) sniffed from <code>.json</code>. Requires <code>pip install \"memotrix[knowledge]\"</code> (<code>rdflib</code>) for RDF parses.</p> <p>These still become text chunks for hybrid search. Memotrix is not a SPARQL engine.</p>"},{"location":"use-cases/knowledge-graphs-and-fhir/#scorm","title":"SCORM","text":"<p><code>.scorm</code> or a zip that contains <code>imsmanifest.xml</code> is parsed as a learning package. Other zips are rejected.</p>"},{"location":"use-cases/postgres-persistence/","title":"Use case: persist memory in PostgreSQL","text":"<p>In-memory HNSW/BM25 dies with the process. <code>PostgresStore</code> writes chunks to PostgreSQL + pgvector.</p> <pre><code>pip install \"memotrix[postgres,memory]\"\n</code></pre> <p>You must supply a DSN. Example (use your credentials, not a library default):</p> <pre><code>import os\nfrom memotrix import Memory\nfrom memotrix.embeddings import HuggingFaceEmbeddings\nfrom memotrix.vectorstores import PostgresStore\n\nembeddings = HuggingFaceEmbeddings(model=os.environ[\"EMBEDDING_MODEL\"])\nstore = PostgresStore(\n    connection=os.environ[\"DATABASE_URL\"],\n    embeddings=embeddings,\n    table_name=\"memotrix_chunks\",\n)\nmemory = Memory(embeddings=embeddings, store=store)\n</code></pre> <p><code>connection=</code> on <code>Memory</code> without <code>backend=\"memory\"</code> selects Postgres automatically.</p> <p><code>Memory.from_env()</code> requires <code>EMBEDDING_MODEL</code> and, when <code>MEMOTRIX_BACKEND=postgres</code>, a resolvable DSN.</p>"},{"location":"use-cases/postgres-persistence/#schema-created-if-missing","title":"Schema (created if missing)","text":"<p>The store enables <code>pgvector</code> and creates a table sized to <code>embeddings.dimension</code>, with HNSW on the vector column and GIN on <code>tsvector</code>. If you change embedding width, use a new <code>table_name</code> or drop the old table.</p>"},{"location":"use-cases/postgres-persistence/#docker","title":"Docker","text":"<p>Repo <code>docker-compose.yml</code> image: <code>pgvector/pgvector:pg16</code>. Inject <code>POSTGRES_*</code> yourself.</p>"},{"location":"use-cases/postgres-persistence/#limitations","title":"Limitations","text":"<ul> <li>Network and DB latency dominate vs in-memory.</li> <li>One table per embedding dimension.</li> <li>No built-in backup; use Postgres tooling.</li> </ul> <p>Demo: <code>DemoCodeLive/demos/11_postgres.py</code> (skipped unless <code>DATABASE_URL</code> is set).</p>"},{"location":"use-cases/rag-agent/","title":"Use case: RAG agent loop","text":"<p>Memotrix retrieves. You generate.</p> <pre><code>from openai import OpenAI  # pip install \"memotrix[openai]\" plus openai client\nfrom memotrix import Memory\nfrom memotrix.embeddings import HuggingFaceEmbeddings\n\nmemory = Memory(embeddings=HuggingFaceEmbeddings(model=\"BAAI/bge-small-en-v1.5\"), backend=\"memory\")\nmemory.add_text(\"To deploy Alpha, run `alpha deploy --force`. Never deploy on Fridays.\", memory_type=\"procedural\")\n\nclient = OpenAI()  # needs OPENAI_API_KEY\n\ndef ask(question: str) -&gt; str:\n    hits = memory.search(question, top_k=3, memory_type=\"procedural\")\n    context = \"\\n\\n\".join(h.get(\"chunk_text\") or \"\" for h in hits)\n    response = client.chat.completions.create(\n        model=\"gpt-4o-mini\",\n        messages=[\n            {\"role\": \"system\", \"content\": \"Answer only from the context.\"},\n            {\"role\": \"user\", \"content\": f\"Context:\\n{context}\\n\\nQuestion: {question}\"},\n        ],\n    )\n    return response.choices[0].message.content\n\n# answer = ask(\"How do I deploy Alpha?\")\n</code></pre> <p>You can use <code>OpenAIEmbeddings(model=\"text-embedding-3-small\")</code> instead of HuggingFace if <code>OPENAI_API_KEY</code> is set. Dimension is inferred from the first embeddings API response.</p> <p>The DemoCodeLive RAG script prints the assembled prompt and does not call a paid API unless you uncomment it.</p> <p>See also <code>docs/tutorials/02-build-a-rag-agent.md</code>.</p>"},{"location":"use-cases/structured-data/","title":"Use case: structured data","text":"<p>Spreadsheets, JSON, YAML, SQL dumps, and XML are extracted into text sections, then chunked like any other document.</p> Format Extra Notes <code>.csv</code> none Stdlib <code>csv</code>; rows batched (8 per section) <code>.xlsx</code> <code>excel</code> <code>openpyxl</code> + <code>pandas</code>; <code>.xls</code> is rejected <code>.json</code> none Pretty-printed unless sniffed as FHIR / GeoJSON / chat / JSON-LD <code>.yaml</code> / <code>.yml</code> <code>yaml</code> <code>PyYAML.safe_load</code> <code>.sql</code> none Raw SQL text <code>.xml</code> none Parsed to text <pre><code>memory.add(\"exports.csv\")\nhits = memory.search(\"which donors are in region north?\")\n</code></pre> <p>Limitation. Tabular Q&amp;A is still retrieval over text, not a SQL engine. Precise aggregations (\u201csum of column X\u201d) are not computed by Memotrix.</p> <p>Demo samples: <code>DemoCodeLive/samples/data.csv</code>.</p>"}]}